# session-handoff-2026-09-16 测试环境全量回归（发布正式前最后一遍）

> 2026-09-16 会话交接（新 session 先读）
> 目的：用户要求把「整个项目之前测试反馈的问题」在测试环境再自测一遍，确认健康后由运维发布正式环境。
> 覆盖：Obsidian 索引中全部历史 bug 修复 + v0.2 迭代功能，A–H 共 8 组全部通过，**无遗留缺陷**。

---

## 一、验证结果全览（可直接引用）

| 组 | 验证项 | 结果 |
|---|---|---|
| A | 认证：admin 登录跳转 / cookie+CSRF / iOS 聚焦放大(6a789c4 禁缩放特征) | ✅ 全过（前序会话已验证，本次沿用） |
| B | 学院管理员：教师考试建考(audience radio/班级隐藏/学院锁定) / **学生考试建考(班级必选✓ 学院锁定✓ 本院班级下拉25个✓)** / **跨学院1101拦截(403 数据越权✓)** | ✅ 全过（本次补全学生考试+1101） |
| C | 教师端：登录落/student / 答题25题交卷100分 / 证书文案无"同学" / **证书列表403修复(f1fe1e4)✓** / 证书详情9:16(400×711=16/9精确✓) / **证书返回落点(链路3)✓** / 证书导出(表头含考生类型列✓ 教师行工号回退BN100007612✓ 按工号搜索命中✓) | ✅ 全过（本次补齐列表403/导出Excel解析） |
| D | 分班模式下线(7fb7d53)：学校管理员无教师分班菜单✓ / **学院管理员「本院教师」只读名单(姓名/工号/账号状态三列，无编辑入口)✓** / 教师登录落/student | ✅ 全过 |
| E | 全年级修复(587285a)：教师考试详情无班级项✓ / 列表班级列"按学院"✓ / **学生考试详情班级正常显示(23电子一班)✓** | ✅ 全过（本次补齐学生考试班级） |
| F | 教师端移动端自适应(e8a8967)：**390视口五页(首页/考试管理/本院教师/学院统计/证书导出)无横向溢出✓ / 抽屉开合(menu-toggle transform -260↔0)✓** | ✅ 全过 |
| G | 学生端：证书9:16✓ / **证书返回三链路(考试列表/结果页/证书列表→证书→返回均正确落点)✓ / tab还原(?tab=FINISHED已考tab高亮)✓** / tabbar贴底(fixed+left:0+transform:none+gap0)✓ / 列表回顶容器(100dvh固定+overflowY auto)✓ / safe-area补偿(calc(50px+env))✓ | ✅ 全过（本次补齐链路1/2+CSS特征） |
| H | API/DB：**切屏强制交卷(e627ab8: 第3次LEAVE才forceSubmitted=true,卷505aae97 AUTO_SUBMITTED)✓ / 重考拦截(082d471: 终态卷重复submit/存答案均1403)✓ / 判断题判分(a4c94f0: 快照A/B存储+100分含判断题全对)✓ / 统计参与率(7942571: 学院页0.3%≤100%)✓ / 1404静默(51a780b: 已交卷上报LEAVE→ignored:true)✓ / 组卷去重(0676156: 两卷各25题题干全唯一)✓** | ✅ 全过（本次全部API/DB级实测） |

## 二、本次会话关键实测证据

1. **教师证书全链路**：李天翔(BN100007612)答25题→API交卷100分→证书 LAB-2026-DQ-xZIAvIhU 自动签发→证书列表2张可见（403修复后）→详情文案「恭喜 李天翔 ，」无"同学"→9:16→返回列表。
2. **证书导出 Excel 解析**：调 `/api/certificates/admin/export` 下载 xlsx，表头=姓名|学号/工号|考生类型|学院|班级|考试|证书编号|发证日期；教师行学号列=BN100007612（工号回退）、考生类型=教师；搜索 BN100007612 命中2张。
3. **学生考试建考**：考生类型选「学生(按班级)」→不选班级点确认→校验报「请选择班级」；班级下拉25个均为本院班级；学院 select disabled 锁定本院；发布后详情页班级=23电子一班。已删除该草稿（不保留）。
4. **跨学院 1101**：薛晓转(管电气学院)访问汽车学院考试 31da037c → 403/1101 数据越权；本院考试200。
5. **切屏强制交卷**：重考开卷后连续3次LEAVE，返回 leaveCount=1/2 forceSubmitted=false、第3次 forceSubmitted=true；DB卷505aae97=AUTO_SUBMITTED。
6. **1404静默**：已交卷考试上报LEAVE → 201 `{"success":true,"ignored":true}`，不弹1404。

## 三、⚠️ 本次新踩坑（务必记录）

- **浏览器多 tab 会话共享**：lab-exam cookie 认证模式下，同一浏览器 profile 的多个 tab 共享登录 cookie。用 tab2(李天翔) 做完教师验证后，在 tab3 登录薛晓转，**tab2 的会话被顶替为薛晓转** → 已考列表变空、曾误判为 bug。排查方法：任何列表为空先 `fetch('/api/auth/me')` 确认当前会话归属。切换账号需先 logout 或新开 profile。
- 事件上报 DTO 字段是 `clientTime`（不是 occurredAt）；`events` 接口白名单只有 HEARTBEAT/BLUR/FOCUS/LEAVE/ANSWER/SUBMIT。

## 四、测试环境当前状态（192.168.0.165:13306）

- 前端构建：`index-Lnq_tEi1.js`（09-07 v0.2 最新，= main 587285a）。
- 测试数据（看完可删）：
  - `0916回归-教师考试`（PUBLISHED，电气学院，audience=TEACHER，李天翔已考2次：首考100分/证xZIAvIhU + 切屏验证卷505aae97 0分AUTO_SUBMITTED）
  - `V02EXP-教师准入考试`（CLOSED，李天翔100分/证VQOpn5HN）
  - `教师考试0907`（CLOSED）
  - 学生考试草稿已删除；薛晓转本场建的其他草稿见 sheets（无）
- 账号（测试环境）：admin=oceg2026、薛晓转 100029018=oceg2026（COLLEGE_ADMIN+TEACHER，管电气）、李天翔 BN100007612=V02@2026pwd（纯教师）、陈卓 IME000179=Test@2026v2（**当前只有TEACHER角色**，落/student 是正常行为——COLLEGE_ADMIN 角色已不在 user_roles，勿按旧文档当成 bug）。

## 五、正式发布前 checklist（v0.2 发正式，待运维执行）

1. 备份 exams 表后执行 `D:\claude-work\lab-exam\04_add_exam_audience_prod.sql`（exams.audience 列）。
2. 前端构建带 `VITE_AUTH_MODE=cookie`。
3. 发版后正式环境回归：学院管理员建教师考试→教师考试→教师证书导出（正式库现无教师证书需造数）；回归清单可复用本 session 的 B/C/E 组步骤。
4. Jenkins build 部署前确认无 IN_PROGRESS 考试（撞重启会 timeout 但数据零丢失，见 09-07 程仁轩事件）。

## 六、遗留（非本次缺陷）

- 正式库 2001 道题 content 错位修复 SQL `03_fix_questions_prod.sql` 待运维执行（09-01 已生成校验）。
- 学校管理员 fix-school-admin-password.sql 待拍板；BN100005453(沈春洋) 正式密码仍为 Gb$d!a$X8&eTbij4。
- 测试环境交卷慢问题：判断为跨网络 RTT 叠加串行 round-trip，无代码问题，待用户提供实测数据继续定位（不阻塞发版）。