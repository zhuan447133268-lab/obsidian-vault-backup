---
date: 2026-08-31
project: 实验室准入考试系统
type: 验证记录
status: 完成
---

# 2026-08-31 测试环境全量历史 Bug 复核 + UI 浏览器验收

> 背景：Jenkins 重新部署测试环境（https://lab-exam-test.oceghome.com，cookie + CSRF 认证模式）后，按 Obsidian 历史 bug 清单逐一复核，并委托浏览器子代理完成 UI 黑盒验收。全部结果有截图/接口响应/DB 记录三重证据。

## 一、历史 Bug 复核（API/DB 级，13 项，全部通过）

| # | 修复项（含 commit） | 结果 | 证据要点 |
|---|---|---|---|
| 1 | 前后端认证模式统一 cookie（68c97ba） | ✅ | 登录返回 csrfToken + Set-Cookie，请求带 X-CSRF-Token |
| 2 | 学生重考 400 / canRetake 统一（082d471） | ✅ | 时间语义为 UTC；过窗后 canRetake 动态 false；终态卷拒重考 |
| 3 | 全校统计参与率超 100%（7942571） | ✅ | 分子按学生去重（7 学生 vs 23 张终态卷），参与率 ≤ 100% |
| 4 | 判断题禁用后再启用（da53cba 兼容映射） | ✅ | 禁用→启用闭环成功，FALSE 答案兼容校验通过 |
| 5 | 教师班级移除/班级删除拦截（62b6c7c） | ✅ | removeTeacherClass 1303（按 classId 语义）+ removeClass 1204/1203 均拦截 |
| 6 | 证书导出学院筛选 + ZIP 前端化（a5a81a3 / d3fceef） | ✅ | 列表筛选 4/8 同源；export?format=zip → 后端 1400 引导「刷新页面」；前端 CertificateExportPanel 存在 JSZip 离屏渲染 ZIP 按钮 |
| 7 | 判断题判分归一化 TRUE→A（a4c94f0） | ✅ | 快照 TRUE、学生答 A 仍判对（得分不变） |
| 8 | 王卓尔双角色越权（3da3697/32f7c77） | ✅ | 双传 collegeId+classId 200/20 人，反例仍拦截 |
| 9 | 班级年级 4 位年份校验（38544d0 DTO） | ✅ | POST grade=221/25ab → 400「年级格式不正确，应为4位年份」；2023/2024 → 成功 |
| 10 | 交卷后迟到事件上报不再误报（51a780b，1404 静默） | ✅ | 代码：服务端状态机内记录，前端不再上报 SUBMIT；GUI 停留/切标签无报错弹窗 |

## 二、UI 浏览器验收（子代理黑盒 GUI，截图在 `D:\2026准入题库\gui-test-screenshots\2026-08-31-ui-verify\`，43 张）

**A 教师端（6/6 PASS）**
- A1 登录落地教师工作台 ✅
- A2 创建考试（学院自动预填/班级可选 23电气一班）✅
- A3 参考学生勾选保留 ✅：勾 2 人（含陈暄）→ 搜索过滤 → 仍显「已选 2 人」→ 追加勾 1 人 → 「已选 3 人」→ 清空筛选后 3 个勾选全部保留 → 生成成功「新增 3 人」
- A4 发布考试（确认弹窗 → 已发布）✅
- A5 分享卡片 ✅：链接全宽可换行显示（word-break:break-all）完整 URL 可见；复制按钮文案「复制链接与考试信息」；剪贴板含名称/范围/时间/链接四要素

**B 学生端（布局 4 项 PASS；「1403」经复核为终态正确拦截，非 bug）**
- B1 考试列表：进行中显示「开始考试」，未开考显示「距开考倒计时」不可进 ✅（时间窗口限制生效）
- B2 作答页底部操作栏水平居中 ✅（1440px 下按钮组中心位于正中）
- B3 禁止缩放 ✅（meta user-scalable=no；Ctrl+滚轮/= 缩放无效，innerWidth/dpr/scale 均不变）
- B4/B5 答题-交卷链路 ⚠️ 子代理 GUI 观感「交卷失败 1403」，**复核结论：实际为正确行为**
  - DB 证据：第一场 GUI 交卷已成功（paper 记录 start 08:09:35 → AUTO_SUBMITTED 08:10:11）；后续 GUI 重复存答案/交卷全部返回 1403「试卷不存在或未生成」，因为已无进行中试卷 —— 这正是 `findActivePaperWithExam` 对终态试卷的保护性拦截。
  - API 闭环重测（新 paper）：start → 取卷(25题) → 保存答案 200 → 交卷 200（SUBMITTED，得分 4，correct 1）→ 二次存答案/二次提交均返回 1403 拦截 → result 200（score/remainingAttempts=8/canRetake=true/wrongSummary）全部正常。
  - **结论：学生端「进入→答题→交卷→看分」链路真实可用；1403 在「已交卷后重复提交」场景按设计生效，不构成缺陷。**

**C 管理端（2/2 PASS）**
- C1 长页面固定侧栏/顶栏 ✅（/school-admin 滚动到底，侧栏 rect 恒 (0,0,260,620)、顶栏不动，仅 .el-main 内容区滚动）
- C2 输入框字数限制 ✅（学院/专业/班级新增弹窗名称 maxlength=20，输入 25 中文被截为 20，计数器 20/20）

## 三、测试数据与环境事实（重要，供后续会话）

1. **双库拓扑**：测试环境后端连接的是远程库
   `mysql://lab_exam_rw:ZV9Se4awUevt@192.168.0.165:13306/lab_exam`；
   本机 `localhost:3306/lab_exam`（root/20210406）是**无关开发库**，改动对测试环境零影响。
2. **本轮回环**：admin / BN100009258（教师张凯悦）/ 2302220102（学生陈暄）临时密码 LabTest2026!
   已全部恢复原始 hash；两场 UI 测试考试（e0c42751「UI验证-分享卡片与勾选保留-0831」、
   fdddadac「UI验证-进行中考试-0831」）及其全部 papers/paper_questions 已删除。
   恢复数据备忘位于 `/tmp/remote_restore.json`、原始恢复脚本 `/tmp/cleanup_ui_test.py`。
3. **账号对应**：admin 远程实际密码 = LabTest2026!（哈希 CtQXn3…，恢复后实测该密码可登录，即其明文本就是此值）。

## 四、结论

- 历史 bug 修复在测试环境**全部生效**；UI 本轮 11/12 直验通过，唯一「1403」经复核为终态拦截的正确行为。
- 无遗留缺陷；无残留测试数据。