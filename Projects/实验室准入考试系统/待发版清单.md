---
title: 待发版清单（下次发版一并发版）
date: 2026-09-20
project: 实验室准入考试系统
tags:
  - lab-exam
  - 发版
  - 待办
---

# 待发版清单 —— 攒批发版（2026-09-20 起）

> **决策（2026-09-20 用户拍板）**：本批（改密入口 + 学校管理员重置规则）**不单独发版**，记为待办，**等业务方提出新问题后一并发版**。此前 2026-09-18 也定过"待下次迭代一起 build"（见 [[索引]] 六 §）。
> 用途：发版日按本清单逐条核对，避免遗漏已复验但未上正式的改动。仓库侧闸门仍以 `deploy/RELEASE_CHECKLIST.md` 为准。

## 一、正式环境现状（2026-09-20 只读指纹实测，非推测）

| 探测项 | 实测 | 判读 |
|---|---|---|
| 入口产物 | `index-D1EUAOW4.js`（= 09-18 测试环境改版前的基线同一入口） | 入口 chunk 未变（入口 hash 只随入口自身内容变，`statistics` 等分片单独变） |
| `statistics-C600IaRS.js` | 含「**下方**」文案（无「上方」） | `2834ac8` **已在线** |
| 同上 | 仍含写死的「**含学生与教师**」 | `00129a0` **未上正式** ⇒ 正式学院统计页仍是旧提示 |
| 全量 62 个产物 js | **无** `isSystemAdmin`、**无** `user-trigger`、**无** `_csrfRetried` | `aaa994e` / `1a055d8` / `df072d5` 均**未上正式** |
| `index-BPLXbD6i.css` | 含 `safe-area-inset-bottom` | 09-03 安全区修复已在正式（说明正式构建较新，边界可信） |

**结论：正式环境 = 「2834ac8 及之前」的构建**；下列 8 个 commit 全部还在等发版。

## 二、待发版 commit 清单（`2834ac8..aaa994e`）

| commit | 类型 | 内容 | 测试环境状态 | 发版后正式环境需验的点 |
|---|---|---|---|---|
| `aaa994e` | 后端 | 学校管理员密码仅应急系统管理员 `admin` 可重置（1507 新文案 + `isSystemAdmin` 下发） | ✅ 09-20 复验 13 项全过 | `admin` 能重置沈春洋/陈冰冰；普通学校管理员重置同级仍 `1507`；`admin` 重置自己 `1506` |
| `1a055d8` | 前端 | 管理端/教师端顶栏账号菜单「修改密码」+ 改密页「返回」 | ✅ 09-20 复验 | 学校管理员点自己头像能进改密页并返回原页 |
| `df072d5` | 前端 | CSRF 失效(1004)静默自愈（换新 token 前核对身份后重试一次） | ✅ 09-18 复验 | 多标签页/顶号场景操作不再随机报错（**旧红线：不得跨账号盲目重试**） |
| `531254c` | 后端 | 题库仅切换「启用状态」时跳过内容校验（存量脏数据不再卡启用开关） | ✅ 09-18 复验 | 老师能正常启停题库题目（配套 248 条脏数据清洗 SQL 已于 09-18 由运维在正式库执行完） |
| `00129a0` | 前端 | 学院统计页明细口径提示跟随「考生类型」 | ✅ 09-18 复验 | 正式学院统计页明细标题不再写死"含学生与教师"（**当前正式仍是旧提示**） |
| `bd3d311` | 文档 | 发布清单补迁移账本前置项 | — | 无运行时影响 |
| `6d90623` | 文档 | 升级部署章节补数据库注意事项 | — | 无运行时影响 |
| `49b0617` | 文档 | 发布清单补三项防复发检查 | — | 无运行时影响 |

**没有任何数据库结构变更**（8 个 commit 全为代码/文档），因此发版不需要新增迁移；但发版前置仍要按 `RELEASE_CHECKLIST.md` 核对正式库 `_prisma_migrations` = **8 条**（不足先 `migrate resolve --applied` 补登记，否则 `migrate deploy` 报 P3009）。

## 三、发版当天必须记住的三条

1. **push 不触发 Jenkins，需手动 build**；前端构建必须注入 `VITE_AUTH_MODE=cookie`（否则正式登录态失效）。
2. 正式库迁移账本应为 **8 条**（09-18 已由运维补齐；旁证：`paper_generation_jobs` 仍未进迁移，见下方待办）。
3. 部署后先跑 `GET /api/exams` 应 200（09-18 事故的哨兵接口），再做上面的功能复验。

## 四、发版时一并交代运维的事

- **`admin`（应急系统管理员）密码须由运维单独保管**（正式为 `oceg2026`）：`aaa994e` 生效后它是**唯一**能重置学校管理员密码的账号；`admin` 自己忘记密码无解（1506），钥匙丢了就没有第二把。

## 五、仍待拍板/缓做（不阻塞本次发版）

- [ ] **学院管理员能否重置本院教师**（用户 2026-09-20 表示"再说"）：现状只有学校管理员能重置；正式环境目前 **0 个学院管理员**，故不紧急。
- [ ] **迁移账本健康度（P2）**：`paper_generation_jobs` 从未进过迁移（`schema.prisma` 有、正式库实测存在、迁移文件 0 命中）→ 建议发版时补一条纯 `CREATE TABLE` 迁移或 `deploy/sql/` 兜底；并统一"schema 变更必须先 `prisma migrate dev` 生成迁移文件"。
- [ ] **提高并发容量三件事**（09-17 容量评估）：DB 与应用同机房 / 显式 `connection_limit`+`pool_timeout` / 合并「存答案 + ANSWER 事件」需产品确认；正式考试前建议做容量预演。
- [ ] **全校统计页也放一张考生明细表**（P3 扩展，待业务确认；后端已支持）。
- [ ] 学校管理员密码 `fix-school-admin-password.sql`（正式库仍是 8-28 旧版，沈春洋/陈冰冰 = 强密码；**今起可由 `admin` 自助重置，该 SQL 优先级下降**）。

## 六、关联

- 避坑 #33（改密入口 + 重置规则）· [[避坑指南-验收清单]] 2026-09-20 批次 · [[lab-exam-2026-09-20-改密入口与学校管理员重置规则]]
- 本批测试环境证据：`gui-test-screenshots/2026-09-20-testenv-reset-rule/`（含 `00-BEFORE-*` 旧构建对照）
- 正式环境只读核查记录：`gui-test-screenshots/2026-09-18-prod-audience-fix/`（09-18 事故闭环）