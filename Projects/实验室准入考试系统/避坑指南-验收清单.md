---
title: 避坑指南 · 测试环境验收清单
date: 2026-08-20
project: 实验室准入考试系统
tags:
  - lab-exam
  - 验收清单
  - 避坑指南
---

# 避坑指南 · 测试环境验收清单

> 目的：把 `避坑指南.md` 里标注「⏳ 未复验」的修复项，逐条在测试环境验收，验收一项勾一项。
> 测试环境：https://lab-exam-test.oceghome.com/ （管理员 admin；本次需覆盖教师/学生角色时用对应测试账号）
> 关联：# 编号与 `避坑指南.md` 中条目一致，详情（坑/根因/修复）见主文件。

---

## 2026-09-18 批次（2 条 · 统计页文案：方向纠错 + 明细口径随筛选）— ✅ 测试环境已复验（2026-09-18，commit `2834ac8` + `00129a0`，产物 `index-2si0ntx2.js`，基线 `index-D1EUAOW4.js`）

> 部署方式：**push 不触发 Jenkins，需手动 build main**；构建后核对下面几处文案——其中 2 处是 hover 提示（原生 `title`），**截图看不到，需读 DOM `title` 属性**。详见 [[lab-exam-2026-09-18-copy-direction-fix]]（本机预览截图 `gui-test-screenshots/2026-09-18-copy-direction-fix/01~03`）。
> **两条可同一次 build 一起验**（都只影响前端文案，无数据库变更）。测试账号：学院管理员 **陈卓 `IME000179` / `Test@2026v2`**（2026-09-18 已把他恢复为**电气电子智能工程学院**管理员，`needChangePassword=false`，可直接登录）。

### #29 提示文案「上方」改「下方」（`2834ac8`）

- [x] **学院统计页（学院管理员账号）**：页头右上角小字显示「导出内容与**下方**「考生类型」一致」（截图 `01-默认全部考生.png`）
- [x] **学院统计页**：「考生类型」下拉 `title` = 「筛选下方考生明细与导出内容，不改变学生/教师 KPI 口径」（读 DOM `title` 属性核对，无「上方」）
- [x] **证书导出页（学院 / 全校两个入口共用 `CertificateExportPanel.vue`）**：「导出内容」下拉 `title` = 「只决定导出范围，不改变**下方**列表；列表的「考生类型」筛选项与导出范围无关」——**两个入口都实测**：学院入口 `/college-admin/certificates-export`（陈卓，截图 `05`）+ 全校入口 `/school-admin/certificates-export`（`admin`，截图 `07`）；页面副提示「仅筛选列表；导出范围由下方「导出内容」决定」同向
- [x] **全校统计页**：「导出范围」下拉 `title` = 「只影响导出内容，不改变**下方**统计口径」（`admin` 登录实测；截图 `06`）
- [x] **反向验证**：三页 KPI 数值与列表/导出行为与修复前**完全一致**（纯文案改动、零逻辑）；`teacher/exam-detail.vue`「请手动选择**上方**文本复制」**保持不变**（该处经核对是对的）——学院页 KPI 在「全部/仅学生/仅教师」三态下逐格相同（学生 1132 / 0.1% / 0.0% / 6，教师 60 / 3.3% / 50.0% / 3）；全校口径导出 206/180/26 = v0.3.5 基线逐数吻合
- [x] **残留扫描**：**全量 63 个产物 js** 逐个抓取统计——「上方」**仅 1 处**（`exam-detail-DNnk-AI8.js`，即应保留的教师考试详情）；「下方」5 处（`statistics-CBxeOncB.js` ×2、`CertificateExportPanel-Doi-flh6.js` ×2、`statistics-BKBzg5QS.js` ×1）

### #30 学院统计页明细口径提示跟随「考生类型」（`00129a0`）

> 背景：明细表与导出共用 `buildCandidatePaperWhere`（audience→userType 过滤），筛选后列表只剩一类，而标题提示写死"含学生与教师"；本改动让提示跟着筛选变。**预期条数以 build 后实测为准**，下面数字是 2026-09-18 当天在测试环境用 `/api/statistics/candidates` 取的参照值（陈卓＝电气学院口径）。

- [x] **全部考生**：`考生类型`不选 → 标题「共 **73** 条（含学生与教师，与导出口径一致）」（截图 `01-默认全部考生.png`）
- [x] **仅学生**：选「仅学生」→ 标题变「共 **50** 条（仅学生，与导出口径一致）」，且列表**无教师行**（截图 `02-仅学生.png`）
- [x] **仅教师**：选「仅教师」→ 标题变「共 **23** 条（仅教师，与导出口径一致）」，且列表**无学生行**——明细表整表 20 行（首页）`考生类型`列**全部为「教师」**，样例行张凯悦 / BN100009258（截图 `03-仅教师.png`）
- [x] **与导出同口径**：任一筛选态下点「导出 Excel」，导出行数 = 标题条数（`GET /api/statistics/export?scope=college&audience=…` 实测 xlsx 数据行 **73 / 50 / 23**，与标题逐数相等）
- [x] **反向验证（KPI 不受影响）**：切换`考生类型`时，上方「学生」「教师」两块 KPI 数值**保持不变**（三态逐格相同：1132 / 0.1% / 0.0% / 6 与 60 / 3.3% / 50.0% / 3）
- [x] **回归**：`考生类型`选「仅教师」且带班级筛选（`2023 23电子一班`）→ 黄色提示「教师考生不属于任何专业/班级，带专业或班级筛选时查不到教师明细；想看教师请先清空专业、班级。」正常弹出、明细 0 条，清除班级后回到 23 条（截图 `04-仅教师+班级筛选-冲突提示.png`）

---

## 2026-09-17 容量基线（非修复项 · 实测记录）— ✅ 测试环境实测完成

> 触发：业务方追问「现在这个项目的并发可以同时多少学生在线考」。方法＝测试环境真实账号走完整考试流程（开考→取卷→存答案→心跳→交卷）+ 并发压测，测完精确回滚。完整记录见 [[lab-exam-2026-09-17-capacity-concurrency-assessment]]。
> **结论**：测试环境现状（单实例 + 应用与 DB 跨网）安全 **200~300 人同时在线**；生产把 DB 与应用同机房后可期 **1000~3000 人**（前提：显式配置 `connection_limit`/`pool_timeout`）。

- [x] **读路径吞吐**：`GET /api/student-exams?status=PENDING` 并发 1/5/20/50/100/200 → 11/23/60/121/**152（峰值）**/93 req/s，全程**零 5xx**（200 并发进入饱和劣化区）
- [x] **写路径单次服务耗时**：开考 858ms（重考含组卷建卷 25 题）、取卷 47ms、存答案 p50 **186ms**、心跳 p50 **208ms**、交卷 576~2221ms
- [x] **单卷并发（最坏情形）**：心跳 50 并发 → 10 req/s / p50 4867ms；存答案 20 并发 → 6 req/s；混合 60 并发 180 请求 → 10 req/s、max 7.6s —— **同一张卷＝`papers` 行锁串行化；真实考试每人一张自己的卷，跨学生不冲突**
- [x] **并发正确性复验（顺带）**：60 并发写同一张卷 → **23/23 答案键全部保留**、交卷仅 1 条终态、未重复发证、零 5xx（v0.3.5 加固在真库真并发下站得住）
- [x] **数据零残留**：卷 7→6、`used_count` 2→1、事件 367→67（300 条随卷级联删除）、证书 0、考试仍 PUBLISHED
- [x] **已排除的瓶颈**：MySQL（`max_connections=1500` / 当前用 10、扫描 8~20ms）、判分 CPU（2 查询 + 内存 25 题循环）、组卷（后台任务 236ms/人、`BATCH_SIZE=5`）
- [ ] **正式环境容量预演（待办）**：发布前用同一方法实测一次 —— 自己账号 → 找 `PUBLISHED` 且未结束、`used_count < max_count` 的考试 → `start` 重考 → 压 `answers`/`events` → `submit` → 删新卷 + 恢复 `used_count`；核验四项残留（卷集合 / `used_count` / 事件数 / 证书数）
- ⚠️ **未测到**：多账号、不同卷的并发写（测试环境只掌握一个学生账号），写路径数字取自单卷最坏情形；400 并发读失败是**本机**连接上限，不是服务端上限
- ⚠️ **测量坑（已写入 `避坑指南.md` #28）**：`GET /api/student-exams` 的参数是 `status` **不是 `tab`**，未知参数被全局 ValidationPipe 直接 **400** —— 第一版压测脚本因此把全部请求打成「拒答路径」（p50 38ms 的"好看数字"其实是全部失败）

---

## 2026-09-17 追加批次（2 条 · 学院统计考生明细 + 考试链路并发加固）— ✅ 测试环境复验通过（commit `c8e911e` → 构建 `index--ZBR24HL.js`）

> 部署指纹（证明真的上了）：前端主包 `index--ZBR24HL.js`（`index.html` Last-Modified `2026-09-17 09:17:41 GMT`）；学院统计分块 `statistics-DcKqXPDm.js` 内含 `candidateType` / `candidateTypeText` / `考生明细` / `仅教师` 标记；后端 `GET /api/statistics/candidates` 已存活（200）。

### #26 学院统计页可直接筛选并看到本院教师考试情况（`c8e911e`，PRD §7.7）— ✅ 2026-09-17 测试环境端到端复验通过

- [x] **验收步骤（老师反馈的原场景）**：学院管理员进「学院统计」→ 「考生类型」选 **仅教师** → 点查询 → 页面**直接出现**「考生明细」表格，行内是本院教师（姓名 / 工号 / 考试 / 结果状态），**不再需要点导出才看得到** —— 测试环境实测：薛晓转（100029018）选「仅教师」后**直接看到 23 行**本院教师明细，全程未点导出（截图 `01`~`04`）
- [x] **验收步骤（可见性证据）**：明细表分页可翻页（>10 行时）、列含 12 项（考生类型 / 姓名 / 学号·工号 / 学院 / 专业 / 班级 / 考试 / 开始·交卷时间 / 时长 / 得分 / 结果状态 / 证书）—— 测试环境实测 **12 列齐全**、23 行分 **3 页**可前后翻（截图 `03`/`03b`）
- [x] **验收步骤（与导出同源）**：同一组筛选条件下，**页面明细行**与**导出 Excel 行**逐行一致（行数相同、关键列相同）—— 测试环境用 exceljs **逐单元格**比对 **6 组**筛选组合（仅教师 / 仅学生 / 全部 / 工号搜索 / 状态=已通过 / 学校口径）→ **6/6 全一致**；导出原件 `07-export-仅教师.xlsx`（23 行）、`09-export-学校-仅导出教师.xlsx`（26 行）
- [x] **验收步骤（搜索）**：按**工号**搜索教师（如 `BN100009258`）→ 只出该教师 —— 测试环境实测工号命中 **9 行**（该教师多场考试的各卷），行内姓名列恒为该教师、无他人混入
- [x] **验收步骤（三档筛选）**：考生类型 全部 / 仅学生 / 仅教师 三档分别查询，行数与「导出内容」对应档位的导出行数一致 —— 测试环境实测明细接口 **全部 73 / 仅教师 23 / 仅学生 50**，与同条件导出逐档吻合
- [x] **验收步骤（全校口径）**：学校管理员（陈冰冰 IME000290）进「全校统计」→ ⚠️ **该页本就只做 KPI 与图表，没有「考生明细」表格**（本批设计范围＝学院统计页，**不是缺陷**；原清单此条描述有误，已按实测修正）—— 学校管理员调明细接口实测 **206 = 学生 180 + 教师 26**，数字自洽口径可用；「全校统计页也放一张明细表」属**另立的 P3 扩展项，本批未实现**
- [x] **权限回归**：跨学院查询、CLASS/SELF 范围访问该接口应返回 **1101**（无权限），前端不暴露该表 —— 测试环境三向对照实测：纯教师账号 → **403 / code 1100**；学院管理员传**非本管理范围** collegeId → **403 / code 1101**；传本学院 → **200**
- [x] **口径不变（回归）**：本批**不改** KPI 人数口径与导出卷数口径（KPI=人数 / 导出=卷数，见 PRD §7.6.3），页面 KPI 数字应与上一版一致 —— 复验后明细三档仍为 **全部 73 / 仅教师 23 / 仅学生 50**，与接口层复验完全一致（23+50=73 恰好拼成全集）

### #27 考试链路并发加固（`c8e911e`，PRD §7.8）— ✅ 2026-09-17 测试环境并发实测通过

- [x] **验收步骤（并发开考）**：同一学生同一考试**并发**连续点开考（或接口并发 8 次）→ 只产生 **1 张**试卷、`student_attempts.used_count = 1`、**无 500**（修复前实测 8 并发有 7 次 500）—— 测试环境实测：8 并发开考 **201×8 / 0 个 5xx / 8 个响应同一 paperId / 库内仅 1 张卷 / `used_count=1`**；再加做 **3 轮"每轮 8 并发"**压测，每轮仍是**唯一新卷 + `used_count` 递增 1、2、3、进行中恒为 1**（重考分支 `createRetakePaper` 同样只建 1 张、只加 1 次）
- [x] **验收步骤（并发保存答案不丢）**：考试中两处并发保存不同题目的答案 → 两次答案**都保留**（修复前整列 JSON 读改写会互相覆盖）—— 测试环境实测 **5 个键并发保存后 5 个键全部保留**（`FOR UPDATE` 行锁合并生效）
- [x] **验收步骤（并发交卷只发一次证）**：同一张卷并发/重复交卷 → 只写 **1 条** SUBMIT 事件、只发 **1 张**证书 —— 测试环境实测：8 并发交卷**全部返回 100 分**，库里 **1 条 SUBMIT 事件 / 1 张证书 / 试卷终态 SUBMITTED**；**顺序重复交卷（卷已终态）实测为 `400 / code 1403「试卷不存在或未生成」`且零写入**（状态、分数、submitTime、事件数全不变）—— 原清单写的"返回已终结状态而不再报 1404"**表述不准，已按实测修正**；前端交卷成功后即停止上报，该错误不会弹给考生
- [x] **验收步骤（强制交卷撞车）**：切屏第 3 次触发强制交卷的同时手动交卷 → 只有 **1 条**终态事件、分数唯一 —— 测试环境实测 **3 轮**，每轮事件分布均为 `PAGE_HIDE×3 + SUBMIT×1`（每轮恰好 1 条终态、分数唯一）
- [x] **验收步骤（超时扫描不覆写）**：定时任务（每分钟）与手动交卷撞车时，**不覆写**已存在的终态（原卷状态/分数保持不变）—— 测试环境**两层实测**：① 一张**两个超时条件同时成立**的已交卷卷被扫描器连扫 **2 个周期** → 仍是 `SUBMITTED / 100`、仍 1 条事件 1 张证书；② 让交卷请求**跨分钟 tick**（tick 前 299ms / 后 15ms / 后 308ms）抢跑 3 轮 → **3/3 交卷赢**、均 SUBMITTED、无 TIMEOUT 覆写、无残留 SUBMIT 事件（修复前的特征会是 TIMEOUT + 一条悬空 SUBMIT 事件）
- [x] **验收步骤（次数用尽）**：次数用完后再开考返回 **1407**；`used_count` 不超过 `max_count` —— 测试环境实测：`used_count` 打满 10 后再开考 → **400 / code 1407**，`used_count` 保持 10、**未多建试卷**
- [x] **超时扫描仍在工作（CAS 没把扫描器改坏）**：测试环境自建一场真超时考试、扫描器正常把卷判成 **TIMEOUT / 0 分** —— ⚠️ 注意**超时扫描按设计不写 TIMEOUT 事件**（全库 TIMEOUT 事件总数 = 0、TIMEOUT 卷 6 张），故断言应为"试卷状态变 TIMEOUT + 分数落库"，原清单"1 条 TIMEOUT 事件"的写法有误，已修正
- [ ] **可观测性（回归）**：若后端仍出现未处理异常，服务端日志应有 `未处理异常 METHOD URL: message` + 堆栈（修复前 500 无任何服务端痕迹）—— **外部无法验证**（需运维看容器日志）；间接证据：本轮全部测试请求 **0 个 5xx**
- 本地验收记录（2026-09-17）：真实 MySQL 并发实测 8 并发开考 = 唯一卷 / `used_count=1`、5 并发存答案键全保留、8 并发交卷 = 1 条 SUBMIT、强制交卷撞手动交卷 3 轮各仅 1 条终态；jest **137 通过 / 11 文件** + `nest build` + `vue-tsc -b` 全绿；浏览器实操 7 步 14 张截图（`gui-test-screenshots/2026-09-17-college-stats-teacher-detail/`）
- 测试环境验收记录（2026-09-17）：构建 `index--ZBR24HL.js`。#26 走**接口层（三档口径 / 页面与导出逐单元格 6 组 / 权限三向）+ 委派子代理浏览器端到端 11/11 项**（37 张截图、0 控制台报错、0 失败请求、390px 无页面级横向溢出）；#27 走**测试库并发实测 4 轮**（首轮 12/13 → 修正探针后 5/5、3/3、3/3，含"每轮 8 并发开考"连做 3 轮的重考分支压测）。证据目录 `gui-test-screenshots/2026-09-17-testenv-v035/`（37 张截图 + 2 个导出 Excel）。**自测数据零残留**（`exams/papers/attempts/uitest = 0/0/0/0`、进行中试卷 0、近一小时新卷 0）；复验后明细基线仍 **73/23/50** 与接口层一致；临时探针脚本已从 `%TEMP%` 清理
- ⚠️ **本轮最大收获（时区坑，已写入 `避坑指南.md` 通用清单）**：测试库**会话时区是 `+08:00`**，**直连写时间列必须用 `UTC_TIMESTAMP(3)`**；用 `NOW(3)` 会写成"8 小时后的未来"，导致超时任务判定 `shouldTimeout=false`，验证一开始假失败（差点误判成"定时任务没跑"）

> 注：上方 #25 批次里「考生明细同源」那一项（当时因功能未部署而留空）**已在本批复验后一并勾选**（见 #25 条目）。

---

## 2026-09-17 追加批次（1 条 · 学院统计「状态」筛选导出口径）— ✅ 测试环境复验通过（commit `3a06114`）

### #25 统计导出带状态筛选时不再混入其它状态的行（`3a06114`）— ✅ 2026-09-17 测试环境端到端复验通过

- [x] **验收步骤（老师反馈的原场景）**：学院统计页选 **状态=已通过 + 导出内容=仅导出教师** → 导出 Excel → 文件里**每一行**的「结果状态」都是「已通过」，**不再出现**未通过/缺考/未开始 —— 测试环境实测 **4 行全部已通过**（修复前同一条件为 9 行、其中 7 行非已通过）；导出文件 `passed-teacher.xlsx`
- [x] **验收步骤（负向同验）**：分别选 未通过 / 缺考 / 未开始 + 仅导出教师 → 导出文件行内状态与所选一致 —— 测试环境实测 **9 行全未通过 / 5 行全缺考 / 5 行全未开始**（`failed-teacher.xlsx`、`absent-teacher.xlsx`、`notstarted-teacher.xlsx`）
- [x] **反向验证（不选状态为回归）**：状态留空 + 仅导出教师 → 导出的仍是**每人每卷一行**（含未开始/缺考），与修复前完全一致 —— 测试环境实测 **23 行**，且 4+9+5+5=23 与四个状态筛选各自的行数**恰好拼成全集**（`all-teacher.xlsx`）
- [x] **验收步骤（学生侧同验）**：状态=已通过 + 导出内容=仅导出学生 → 只含学生**已通过**的卷，未通过的那张不再混入 —— 测试环境实测 **6 行全已通过、全为学生**（`passed-student-testenv.xlsx`）
- [x] **口径确认（避免被当成少数据）**：页面上方 KPI = **人数**（按每人最新一卷），导出 Excel 行数 = **试卷数**（每人每卷一行）；同一人多场都通过时，导出行数会多于 KPI 人数，属预期（口径已写进 PRD §7.6.3）—— 测试环境实测同一页：本院教师 KPI「已通过 1 人」而导出 4 行、学生 KPI「已通过 0 人」而导出 6 行（张凯悦 2302220248 有 6 张已通过卷但最新一卷未通过），**这是本次修复刻意保留的人数口径，需向业务方说明**
- [x] **独立核对（查库，不依赖接口自身）**：直接查测试库 `papers × exams × users(user_type=1)` 按 `passScore` 自行推导状态 → 本学院教师试卷 **4 已通过 / 9 未通过 / 5 缺考 / 5 未开始 = 23**，与五个导出文件逐行吻合（`查库-教师试卷状态分布.txt`）
- [x] **验收步骤（学校管理员/全校口径）**：学校管理员（陈冰冰 IME000290）进「全校统计」选 **状态=已通过 + 仅导出教师** → 导出 **5 行全部已通过、全为教师**（含非本院教师），全校口径同样生效
- [x] **验收步骤（考生明细同源，随该功能上线）**：学院统计页新增的「考生明细」表格用同一套条件筛选时，行内状态与导出 Excel 逐行一致 —— **2026-09-17 随 v0.3.5（commit `c8e911e` → 构建 `index--ZBR24HL.js`）已上线并复验通过**：状态=已通过时页面 4 行、导出 4 行状态全「已通过」，另 6 组筛选组合（仅教师/仅学生/全部/工号/学校口径）页面与导出**逐单元格一致（6/6）**
- 本地验收记录（2026-09-17）：新增单测 `statistics.export-status.spec.ts` 7 例先红后绿（改动前 6 例失败）；两棵树全量套件 137 / 124 例全绿、`nest build` 通过；内置浏览器实操本地栈（`localhost:5175` → 后端 3000 → 本地沙箱库）6 个场景，导出文件逐个解析核对；证据 `gui-test-screenshots/2026-09-17-stat-export-status/`
- 测试环境验收记录（2026-09-17）：构建 `index-4uG3ZrBk.js`；学院管理员 `100029018`（薛晓转）与学校管理员 `IME000290`（陈冰冰）两个视角、7 个导出场景全部解析逐行核对；证据 `gui-test-screenshots/2026-09-17-testenv-v034/`（3 张截图 + 7 个导出 xlsx + 查库结果）
- 本地沙箱数据已还原（临时用户/考试/试卷行已删、`陈卓` 密码 hash 还原）；测试环境**未改任何数据**（只读查库 + 页面导出）

---

## 2026-09-17 追加批次（1 条 · 证书导出范围与 ZIP 归类）— ✅ 测试环境复验通过（commit `0d9b8d4`）

### #24 证书导出可指定范围、ZIP 按班级分文件夹 — ✅ 2026-09-17 测试环境端到端复验通过

- [x] **验收步骤（导出内容三档）**：证书导出页「导出 Excel」左侧出现带标签的「导出内容」下拉（导出全部考生 / 仅导出学生 / 仅导出教师）；三档分别点「导出 Excel」→ 导出条数分别为 **全部（学生+教师） / 仅学生 / 仅教师**，Excel「考生类型」列 100% 对得上 —— 测试环境实测 **8（4 学生 + 4 教师） / 4（全学生） / 4（全教师）**（`cert-all.xlsx`、`cert-student.xlsx`、`cert-teacher.xlsx`）；后端请求分别不带 `audience` / `audience=STUDENT` / `audience=TEACHER`
- [x] **验收步骤（选中即生效）**：切换「导出内容」**不需要先点【查询】**，导出即按新范围走；切换它**不改动上方列表**（列表行数、筛选条件均不变）—— 测试环境实测全程**未点过【查询】**即按新范围导出，且切换过程中列表始终 8 行不变
- [x] **反向验证（与列表筛选互不干扰）**：先把列表「考生类型」筛成「教师」点查询（列表只剩教师），「导出内容」保持默认「导出全部考生」→ 导出的是**学生+教师全部**；再把「导出内容」切「仅导出教师」→ 导出只有教师 —— 测试环境实测列表 **4 行**时，前者导出 **8 行（4 教师 + 4 学生）**，后者 **4 行全教师**（`list-teacher-export-all.xlsx`、`list-teacher-export-teacher.xlsx`）
- [x] **验收步骤（ZIP 目录结构）**：点「导出 ZIP」下载后解压 → 顶层是**班级名文件夹**（每个班一个），教师统一在 `教师/` 文件夹；图片名为 `姓名_学号.png`，教师为 `姓名_工号.png` —— 测试环境实测：`23电气二班/张凯悦_2302220248.png`（学生 4 张）、`教师/张凯悦_BN100009258.png`、`教师/李天翔_BN100007612.png`（教师用**工号**、无班级）
- [x] **验收步骤（ZIP 包名与范围一致）**：ZIP 包名为 `证书_YYYY-MM-DD_全部考生.zip` / `_仅学生.zip` / `_仅教师.zip`，与「导出内容」一致 —— 测试环境实测 `证书_2026-09-17_全部考生.zip`（1.8 MB / 8 张）、`证书_2026-09-17_仅教师.zip`（868 KB / 4 张）
- [x] **反向验证（重名不丢图）**：同一学生**多场考试都通过**（证书多张）时导出 ZIP → 图片不会互相覆盖（重名的自动补考试名/序号），张数与「导出内容」匹配 —— 测试环境**真实命中**：张凯悦（2302220248）4 张学生证书 → `张凯悦_2302220248.png` + `_测试.png` + `_电子技术.png` + `_运动控制系统.png`；教师张凯悦 2 张 → 1 张原名 + `_测试第四个.png`；全部考生 ZIP 内**无重名、无覆盖**（unzip 逐条列出 10 项）
- [x] **ZIP 条数取导出范围的实际条数（原 `total` 误用已修）**：测试环境实测 ZIP 前先发 `page=1&pageSize=1` 计数请求、再按 `pageSize=200` 拉取，两者都带 `audience=TEACHER`；列表当时是 8 行而 ZIP 内只有 4 张 → **条数确实来自导出范围而非列表**
- [ ] **验收步骤（ZIP 上限提示）**：导出范围超过 500 张时点「导出 ZIP」→ 提示改为"浏览器逐张渲染、条数越多越慢越易中断，请按班级或考试分批导出，或改用导出 Excel"，**不发请求** —— 测试环境**数据量不足（仅 8 张证书）无法触发**，本轮未验证；判断源=导出范围实际条数已由上一条实测佐证
- [x] **验收步骤（手机端筛选区）**：手机/390px 视口打开证书导出页 → 学院/考试/班级/考生类型四个筛选项**逐行铺满**、都能点到，不需要整页横向滑动；导出区「导出内容」有可见文字标签 —— 测试环境 390×844 实测：5 个筛选项 + 「导出内容」**全部落在可视区内**（left 33 / right 349 ≤ 390）、`documentElement.scrollWidth=382`、**无横向滚动链**，截图 `02-证书导出页-390px.png`
- ⚠️ **新增观察（未修，低危）**：ZIP 的包名是在**生成完成那一刻**才按当时的下拉值拼出来的。若在生成过程中把「导出内容」切走，包名会跟着变，可能出现"包名写着全部考生、内容其实是教师"的不一致 —— 本轮测试中真实触发过一次（包名 `证书_2026-09-17_全部考生.zip`、内容 4 张教师证书）。正常操作（点导出后不切下拉）不受影响；若要彻底消除，只需在点击那一刻把范围标签快照下来供包名使用
- 本地验收记录（2026-09-17）：`vue-tsc --noEmit` + `npm run build` 全绿（分块 `CertificateExportPanel-BtRfxWjL.js`）；端到端用本机 Edge + `web/node_modules/playwright-core` 直连本地栈（`localhost:5174` → 后端 3000 → 本地沙箱库），6 张证书（学生 5 分属 5 个班 + 教师 1）跑完上述各步；证据 `gui-test-screenshots/2026-09-17-cert-export-scope/`
- 测试环境验收记录（2026-09-17）：构建 `index-4uG3ZrBk.js`，分块 `CertificateExportPanel-DElb9oKO.js` 含「导出内容/仅导出学生/仅导出教师/未分班/按班级分文件夹」全部标记；学院管理员（100029018）与学校管理员（IME000290）两个视角各跑一轮；证据 `gui-test-screenshots/2026-09-17-testenv-v034/`（截图 + 5 个 Excel + 2 个 ZIP 原件）
- 本地沙箱数据已还原（临时教师证书与试卷行已删、`admin` 密码 hash 还原）；测试环境**未改任何数据**

---

## 2026-09-17 追加批次（1 条 · 建考连点防重）— ✅ 测试环境复验通过（commit `87dd9dc` → 构建 `index-DAoTsN6H.js`）

### #23 建考确认连点不再生成重复草稿 — ✅ 2026-09-17 测试环境端到端复验通过

- [x] **验收步骤（复现老师场景）**：用**学院管理员**账号进「教师首页 → 创建考试」，填完名称/班级/开始时间后，**在页面缓慢时连续点「确认」5 次** → 只发出 1 个 `POST /api/exams`，列表只多 1 条 —— 测试环境注入 2 秒延迟连点 5 次：**1 个 POST、HTTP 201、数据库该标题 1 行**
- [x] **验收步骤（按钮即时反馈）**：第一次点「确认」后按钮**立刻**进入 loading 并禁用（不是等响应回来才变），二次点击点不动 —— 实测首击后 `disabled=true` + `is-loading`
- [x] **反向验证（改名不误伤）**：同一账号、同一学院班级，只把考试名改掉再建一场 → 正常建出**新的一条**（防重不能连正常建考一起挡掉）—— 接口层实测改名后返回新 id
- [x] **反向验证（失败后可重试）**：构造一次建考失败（如开始时间早于当前时间 → `1407`）→ 弹窗保留、已填内容不丢、按钮恢复可点，修正后可再次提交成功 —— 测试环境首轮如实撞上 1407，弹窗留存且按钮 `disabled=false`
- [x] **验收步骤（后端幂等兜底）**：同一份建考数据连续/并发提交（绕过前端直调接口）→ 落库仍只有 1 条 —— 串行两次返回**同一 id**、并发 5 次**唯一 id = 1**
- [x] **验收步骤（其它弹窗未被改坏）**：学校管理员「学院管理员设置 → 新增学院管理员」与「师生管理 → 新增师生」各连点 4 次 → 各自只发 1 个请求、成功路径弹窗正常关闭、失败路径弹窗保留且按钮释放 —— 测试环境新增学院管理员（选已存在配对，零数据变更）连点 4 次 → 1 个 POST + 弹窗关闭 + `college_admins` 27 行未变
- [x] **验收步骤（同类入口）**：学院/专业/班级「保存」与「题库导入」「基础数据导入」在提交中按钮转为 loading、重复点击不再发第二个请求
- [x] **验收步骤（产物指纹）**：`index-*.js` 之外要核对**分块字符串**——弹窗组件分块含 `confirmHandler`、四个调用方分块各含 `confirm-handler`，且全量分块**搜不到旧写法 `onConfirm`** —— 构建 `index-DAoTsN6H.js` 实测命中 `AppFormDialog-Cbc3resU.js` + `teacher-D5-RBHpi.js` / `users-3XPbI033.js` / `college-admins-C15mCfmz.js` / `school-admins-CiDRpVAy.js`
- 本地验收记录（2026-09-17）：`npx jest` **117 例 / 10 suites**（新增 4 例防重单测）、`vue-tsc` + `nest build` + `npm run build` 全绿；接口层串行/并发 5 次均同一条考试；浏览器注入 2 秒延迟连点 5 次 → 1 个 `POST /api/exams`、列表与 DB 各 1 行；失败路径（注入 500）→ 1 个请求、弹窗保留、按钮释放。截图 `gui-test-screenshots/2026-09-17-dedup/02~09`
- 测试环境验收记录（2026-09-17，构建 `index-DAoTsN6H.js`）：学院管理员（`100029018`）在 `/teacher` 连点 5 次 → **1 个 POST、201、成功关弹窗、DB 1 行**；接口层串行两次同 id / 并发 5 次唯一 id=1 / 改名新建；回归新增学院管理员（`IME000290`，选已存在配对 李卓 + 电气电子智能工程学院）连点 4 次 → 1 个 POST + 关弹窗 + `college_admins` 27 行未变。截图 `gui-test-screenshots/2026-09-18-testenv-dedup/01~05`。**自测数据零残留**：UITEST 考试已删（列表回到 34 条）、`李卓 need_change_password` 仍为 1 未被改动（改用本就免改密的 `100029018` 账号）
- **现场实锤（测试库分组查询）**：同名「测试无老师」**12 条 DRAFT**，落库 `10:19:25.440 → 10:19:37.412`，其中 `37.406/409/409/412` **4 条挤在 6 毫秒内** —— 即本次反馈的重复草稿。**未删除，等业务方决定**（建议保留 `created_at` 最早那条、删其余 11 条）

---

## 2026-09-17 批次（1 条 · ✅ 测试环境复验通过，含 v0.3.2 排版补验）

### #22 统计与证书导出纳入教师数据（教师独立维度）— ✅ 2026-09-17 测试环境复验通过（commit `6466abb`，构建 `index-BSkJxAPJ.js`；v0.3.2 排版修复 `fe87ad6` → 构建 `index-DbG_pRdd.js` 同日复验通过）

- [x] **验收步骤（全校统计）**：打开「全校统计」→ KPI 分「学生」「教师」两块（教师总数/参与率/通过率/缺考），数字不与学生混算；下方新增「各学院教师统计」表，含无教师学院 0 行 —— 实测学生 8477/0.1%/62.5%/114、教师 306/1.0%/33.3%/3，「各学院教师统计」10 行
- [x] **验收步骤（全校统计收敛）**：考试下拉选一场**教师考试** → 学生口径全部归零（学生总数 0、通过率 0），教师维度收敛为该场实际组卷教师；换回学生考试 → 教师维度归零、学生维度恢复 —— 测试7 → 学生 0/0.0%/0.0%/0、教师 1/0.0%/0.0%/0
- [x] **验收步骤（学院统计）**：「各班级统计」表末尾出现「本院教师」行，数字 = 本院教师 KPI 块；表头人数列对教师行显示教师总数 —— 李卓：学生 1132/0.1%/0.0%/6、本院教师 60/3.3%/0.0%/3，表末行 `本院教师 — — 60 2 0 3 3.3% 0.0%`（26 行）
- [x] **反向验证（学院统计）**：选择任一「专业」或「班级」后「本院教师」行消失（教师不属于专业/班级）；清空筛选后重新出现
- [x] **反向验证（学生口径不被污染）**：任选一人「有教师试卷混入」的考试口径，学生通过率不出现 >100%，且与改前同口径（仅剔除教师）一致
- [x] **验收步骤（证书页）**：证书导出页出现「考生类型」下拉（学生/教师/全部）；选「教师」只出教师、选「学生」只出学生；列表新增「考生类型」列（教师行带标签），学号列显示教师工号 —— 教师 4 行、学生 10 行、全部 14 行（=10+4）
- [x] **反向验证（证书页）**：选中一个班级 → 教师行全部消失（提示文案说明「教师考生不属于任何班级」）；清空班级后教师重新出现 —— 接口层 classId=23电气二班 → 4 条全学生；`&audience=TEACHER` → 0 条
- [x] **验收步骤（全量导出）**：当前库中证书/成绩数据超过 2000 条时点「导出 Excel（全量）」→ 导出条数 = 匹配总条数（不再截断在 2000），Excel 含「考生类型」列，教师行学号列是工号、班级列是 `-`
- [x] **反向验证（ZIP 上限提示）**：ZIP 图片导出仍保留 500 张上限，超出时提示改用「导出 Excel」
- [x] **验收步骤（统计页「导出内容」v0.3.1）**：全校统计/学院统计的「导出 Excel」旁出现「导出内容」下拉（导出全部考生 / 仅导出学生 / 仅导出教师）；选「仅导出教师」→ 导出明细 100% 教师（工号落在「学号/工号」列、班级列 `-`）；选「导出全部考生」→ 明细含学生+教师 —— 全校页「仅教师」+ 教师考试「测试7」→ `?scope=school&examId=b29ea7d0-…&audience=TEACHER` 恰好 1 行（王洋/BN100008266/教师/测试7）；「全部」同 URL 不带 `audience`
- [x] **反向验证（不动 KPI）**：切换「导出内容」不触发查询，页面上方学生/教师 KPI 与「各班级统计」表数字不变 —— 学院页选「仅教师」后 KPI 仍为 1132/60、表格 26 行不变
- [x] **反向验证（学院统计空导出拦截）**：学院统计先选一个班级（或专业）、再把「导出内容」切到「仅导出教师」点导出 → 只弹提示「教师考生不属于任何专业/班级…」，不发请求、不下载空表 —— 实测只弹提示、无新请求
- [x] **验收步骤（v0.3.2 排版 · 全校统计）**：「全校统计」标题、「学生」「教师」分组标题、两组 KPI 卡片都与筛选卡片**内容**左对齐（各比卡片边框再缩进约 20px），不再贴着卡片外边框；「各学院统计」与「各学院教师统计」两卡之间有约 20px 间距，不再贴死 —— 2026-09-17 测试环境实测 h2 `305..385`、`.kpi-section` `285..1415`（padL 20）、分组标题 `306..1394`、KPI 卡 x=`306/582/858/1134`、两表间距 20
- [x] **验收步骤（v0.3.2 排版 · 学院统计）**：同上（标题/KPI/分组标题与卡片内容对齐）；「各班级统计」与「各专业统计」两卡有间距 —— 李卓会话实测：h2 `305..385`、KPI 卡 `306/582/858/1134`、两表间距 20、KPI 学生 1132 与本院教师 60 未变
- [x] **验收步骤（v0.3.2「导出内容」可见性）**：「导出 Excel」左侧能看到文字标签「导出内容」+ 下拉；确认不选任何内容时导出**默认含学生和教师**（无需手动筛选）—— 两页均有「导出内容」标签与 130px 下拉；测试环境选「仅导出教师」后点导出，只发出 `GET /api/statistics/export?scope=school&audience=TEACHER`
- [x] **反向验证（v0.3.2 窄屏 390px）**：手机宽度打开全校/学院统计 → 标题独占一行（不被挤成两行）、导出内容一行不溢出、页面无横向滚动 —— 两页 390px 实测标题 `33..113` 独占一行、KPI 两列 x=`34/203`、横向溢出 0
- 本地验收记录（2026-09-17）：`npx jest` 113 例 / 10 suites、`vue-tsc`+`npm run build` 全绿；本地浏览器端到端 6 张截图 `gui-test-screenshots/2026-09-17-teacher-dimension/01~06`；API 核对证书 14=学生 10+教师 4、统计导出 201 行含 21 行教师
- 本地验收记录（2026-09-17 v0.3.1 导出内容选择）：浏览器实测全校页选「仅导出教师」→ 页面发出 `GET /api/statistics/export?scope=school&audience=TEACHER`（同 URL 返回 21 行、全教师）；学院页（李卓）选后发出 `scope=college&audience=TEACHER`（18 行全教师）；选回「导出全部考生」→ 请求不带 `audience`；带班级筛选 + 仅教师点导出 → 弹提示且无新请求；测试库临时改动（李卓 `needChangePassword`）已复原为 true。截图 3 张：`gui-test-screenshots/2026-09-17-teacher-dimension/07~09`
- 本地验收记录（2026-09-17 v0.3.2 排版 + 导出内容可见性）：全校页 1440px 实测 h2 `305..385`、`.kpi-section` `285..1407`、`.kpi-section-title` `305..1387`、KPI 卡 x=`305/580/854/1128`（均 = 卡片边框 285 + 20 内边距）、两表间距 20、横向溢出 0；390px 标题独占一行、KPI 两列、溢出 0；学院页（李卓 BN100009014）同结构命中（两表间距 20）。`vue-tsc`+`npm run build` 全绿。截图 `gui-test-screenshots/2026-09-17-teacher-dimension/13~16`（本组截图用本机 Edge + playwright-core 直连 5173 生成——内置浏览器拒绝截图 http://127.0.0.1 页面，报 `browser screenshot activity capture failed for guest`）。测试库临时改动（李卓 `need_change_password`）已复原为 1
- 测试环境验收记录（2026-09-17，构建 `index-BSkJxAPJ.js`）：部署前 `/api/statistics/export?scope=school&audience=TEACHER` → 400「property audience should not exist」，部署后同 URL → 200（该构建的 `statistics-pV92sdcz.js` / `statistics-B8dX-3Pj.js` 均含「仅导出教师」字样）；统计导出 202 行（学生 180 + 教师 22）14 列含「考生类型」；证书接口 TEACHER 4 / STUDENT 10 / 全部 14；学院统计本院教师行与全校 KPI 两块数字与本地一致；归档补充 `gui-test-screenshots/2026-09-17-teacher-dimension/10~12`（10 会话交还 admin、11 全校统计「仅导出教师」KPI 未变、12 证书页「考生类型=教师」→ 查询后 4 行全教师）
- 测试环境 v0.3.2 复验记录（2026-09-17 晚，commit `fe87ad6`，新构建 `index-DbG_pRdd.js`）：交付前先验指纹——两个统计页分块 `statistics-E3uAtF4W.js` / `statistics-n7PUFKdy.js` 各含「导出内容」与 `kpi-section art-card`、两个样式分块 `statistics-OAJSq_UC.css` / `statistics-XQaC2lCK.css` 各含 `kpi-section` 与新加的 `flex-direction:column`（**认字符不认文件名**）。全校统计（admin）1440：h2 `305..385`、`.kpi-section` `285..1415` padL 20、分组标题 `306..1394`、KPI 卡 `306/582/858/1134`、两表间距 20、溢出 0、KPI `8477/0.1%/62.5%/114` 与 `306/1.0%/33.3%/3` 均未变；390：标题 `33..113` 独占一行、KPI 两列 `34/203`、间距 20、溢出 0。学院统计（李卓）：同结构命中，学生 1132、本院教师 60 未变。导出回归：选「仅导出教师」→ 点导出只发出 `GET /api/statistics/export?scope=school&audience=TEACHER`，导出后 KPI 未变。截图 `17~20`（另有本地 `13~16`）。测试库李卓 `need_change_password` 临时置 0 后**已复原为 1**，内置浏览器会话已交还 `admin/oceg2026`

---

## 2026-09-16 批次（1 条 · ✅ 测试环境复验通过）

### #21 草稿期考生被禁用，发布时必须拦下（`d572e1a`）— ✅ 2026-09-16 测试环境复验通过

- [x] **验收步骤**：学院管理员建一场教师考试草稿 → 勾选一位在册教师生成试卷 → 去管理端把该教师账号禁用
- [x] **验收步骤**：回到该考试详情页 → 试卷列表该行出现红色「账号已禁用」标签
- [x] **验收步骤**：点「发布考试」→ 先弹常规「确认发布」，确认后弹出「存在已禁用账号 / 存在 N 名已禁用账号：姓名（工号），无法发布。是否排除这些已禁用账号后继续发布？」
- [x] **反向验证**：上一步点「取消」→ 考试仍为草稿、试卷保留（旧版本这里会直接发布成功，是本条要验的核心）
- [x] **验收步骤**：再点发布 →「排除并发布」→ 状态变「已发布」、已禁用考生的试卷被移除（数据库核对 0 张）、其余考生试卷保留
- [x] **反向验证**：全部考生在册的草稿直接发布 → 无 1408 弹框，正常发布且试卷保留（数据库核对 1 张）
- 复验环境：https://lab-exam-test.oceghome.com ，构建 `index-4e5_-ViV.js`（新增分块 `exam-publish-CIm-HAKh.js`）
- 复验记录：草稿「0916测试环境复验-发布禁用校验」考前禁用 ceshi(BN10000999999) → 取消后仍草稿 → 排除并发布成功；回归场「0916测试环境复验-在册发布回归」于洋(BN100008994) 在册直发 201 PUBLISHED。测试数据已删除，账号状态已恢复。

---

## 2026-08-18 ~ 08-20 批次（12 条中待复验 10 条）

### #9 补考通过考生不应再出现在补考安排列表（`c46730a`）

- [ ] **验收步骤**：找一位「次数用尽且已补考通过」的学生 → 打开补考安排列表 → 确认该生不在候选列表中
- [ ] **验收步骤**：对该生（或任一已通过补考考生）再点「生成补考试卷」 → 确认不会生成新补考试卷（或提示无新增），数据库无新 `isMakeup=true` 试卷
- [ ] **反向验证**：补考未通过（低于及格线）的学生仍可再安排补考；补考卷为 DRAFT/IN_PROGRESS 的学生仍被排除，不重复生成

### #10 基础数据导入「状态」列生效（`b4e1247`）

- [ ] **验收步骤**：教师 Excel 中一人状态填「禁用」、一人留空 → 导入 → 禁用者账号 `status=0` 无法登录/被拦截，留空者默认启用
- [ ] **验收步骤**：学生 Excel 同上一组验证；且学生「状态」列与「学籍状态」列互不影响（学籍状态填「休学」不改变账号状态）
- [ ] **反向验证**：已存在的教师/学生，Excel 改状态为「禁用」→ 导入后账号变禁用

### #11 导入结果按模式展示新增/更新/跳过（`f2fe0a3`）

- [ ] **验收步骤**：UPSERT 模式导入一份含「新建 + 重复更新」的 Excel → 摘要显示「新增 N / 更新 M」
- [ ] **验收步骤**：SKIP 模式导入含「新建 + 重复」的 Excel → 摘要显示「成功导入 N / 跳过 M」
- [ ] **验收步骤**：Excel 含失败行 → 顶部提示变 warning（失败数>0 走警告文案）

### #12 证书导出跟随最后一次查询结果（`8f40d77`）

- [ ] **验收步骤**：按学院/考试等条件查询证书 → 清空筛选框但**不点查询** → 直接导出 Excel/ZIP → 导出条数与表格当前展示一致（不是全量）
- [ ] **反向验证**：清空后再点一次查询 → 导出的是全量（快照随查询更新）

### #13 不在参考名单的学生不可进入考试（`124e21e`）

- [ ] **验收步骤**：组卷时未勾选、但同班/同院的学生，打开分享页链接 → 提示「您不在本场考试范围内，无法查看」（code 1207），看不到考试入口
- [ ] **验收步骤**：该生直调 start 接口 → 返回 code 1207，且不会创建试卷、不扣考试次数
- [ ] **反向验证**：名单内学生扫码/分享页正常显示考试并可进入；有历史终态试卷的学生走重考路径不受影响

### #14 导入切换新 Excel 后请求确实发出（`0b81198`）

- [ ] **验收步骤**：先上传一本 Excel（limit=1 已满）→ 统计出结果 → 不删除再直接选第二本 → 页面统计更新为第二本结果（请求真实发出）
- [ ] **反向验证**：切换后有失败行 → 顶部提示正确展示第二本的失败信息

### #15 统计「状态」筛选与列表/导出口径一致、及格线生效（`d50555c`）

- [ ] **验收步骤**：把某场考试及格线改为 60 → 学生 70 分交卷 → 教师班级统计列表状态列显示「通过/未通过」正确（不再显示「已提交」）
- [ ] **验收步骤**：按「未通过」筛选 → 70 分学生不命中；按「已通过」筛选 → 70 分学生命中
- [ ] **验收步骤**：导出 Excel 的「结果状态」列与页面列表一致；缺考/进行中/未开始分别显示「缺考/进行中/未开始」

### #17 工号/学号仅限数字字母（`64f464d`）

- [ ] **验收步骤**：新增/编辑教师、学生，工号/学号输入含中文、下划线、空格 → 输入框即时过滤（只留字母数字）
- [ ] **验收步骤**：接口直调（绕过前端）传含非法字符的工号 → 后端返回 4xx 校验错误「工号/学号只能包含数字和字母」
- [ ] **验收步骤**：正常字母数字工号可保存成功

### #18 教师导出 Excel「结果状态」列有数据（`dde03cb`）

- [ ] **验收步骤**：教师维度导出 Excel → 「结果状态」列每行有值（已通过/未通过/缺考/进行中/未开始），不再是空列
- [ ] **验收步骤**：与页面列表同人同考的状态一致；「试卷状态」列不消失（两者并存）

### #19 导入再次上传新文件后文件名更新（`21e0613`）

- [ ] **验收步骤**：已传一本后直接选第二本 → 上传列表展示文件名变为第二本（旧文件名被替换）
- [ ] **验收步骤**：上一步导入计数与「#14」一致（第二本结果），页面无残留旧文件

## 上一批次遗留（08-17，2 条）

### #6 统计导出与页面列表口径一致（`b6fec42`）

- [ ] **验收步骤**：教师班级统计带 keyword 搜索（如按姓名/学号搜）→ 导出 Excel → 导出数据与页面列表一致（每生一行、仅含搜索命中的学生）
- [ ] **反向验证**：不带 keyword 导出 → 班级全部学生

### #7 分页无重复/漂移（`43f0693`）

- [ ] **验收步骤**：考试列表/用户列表等有分页的列表页，快速翻多页 → 记录无重复、无丢失
- [ ] **验收步骤**：构造同秒创建的若干条记录（或选数据量大的列表）逐页核对序号/记录集合与总条数吻合

---

## 已完成

- [x] #21 草稿期考生被禁用发布时必须拦下（`d572e1a`）— 2026-09-16 Jenkins 部署测试环境后浏览器端到端 + API/DB 双层验收通过（含「取消后仍草稿」反向验证与「全在册直发」回归）
- [x] #16 全校统计按学院筛选后只显示匹配学院（`ad5e669`）— 2026-08-20 本会话测试环境回归通过
- [x] #20 全校统计默认展示全部学院（含无学生 0 行）（`3ebffee`）— 2026-08-20 本会话 API + 浏览器双层验收通过