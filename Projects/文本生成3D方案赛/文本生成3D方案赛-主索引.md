---
title: 文本生成3D方案赛 — 主索引
---
> **🏷️ 新 session 先读（2026-09-06 起）**：
>
> - 现状与下一步 → [[session-handoff-3d-2026-09-06]]（第 2 天：首次提交已入库评测排队中、复现包就绪、云端已关镜像已存；剩 09-07/09-08 两次提交机会）
> - 首日背景 → [[session-handoff-3d-2026-09-04]]（管线建成、双题集就位、GPU 租用起点）
>
> **铁律：任何 session 接手本项目前，必须先读本文件 + 最新交接文档，禁止只依赖会话内记忆。每次会话结束前，把新进展更新回这里。**

# 文本生成3D方案赛 — 主索引

> 本笔记为 CCF BDCI"文本生成3D方案赛"的唯一主文档。所有关键信息、进展、决策、教训均记录于此，新 session 从这里接续。

---

## 1. 比赛关键信息

| 项 | 内容 |
| --- | --- |
| 赛事 | 2026 CCF BDCI 文本生成3D方案赛（出题方：创鑫激光） |
| 平台 | 中科息壤（非 DataFountain！） |
| 赛题页 | https://www.xir.cn/competition/1171 |
| 数据页 | https://www.xir.cn/competition/1171/dataware （报名后才显示下载入口） |
| 奖金 | 一等 1 名×2万 / 二等 2 名×1万 / 三等 2 名×5千 |
| 报名截止 | **2026-09-08 12:00**（用户已报名成功——能下载数据即证明；队名/队ID 待补） |
| 提交窗口 | 09-02 ~ 09-08，**每队每天限 1 次** |
| 评测 | 09-09~09-20；top10 于 09-21~24 交复现资料；09-30 公布入围；决赛 10-08~31 线上答辩 + FDM 实物打印展示 |
| 有效提交门槛 | 完成 ≥90% 题目；每题可加载模型；manifest/metadata/process 齐全；通过官方格式检查 |
| 评分 | FDM 物理合规 + 多视图语义与视觉（MLLM 评渲染图 vs prompt）+ 人工抽查复核 |
| 反作弊红线 | 极简几何骗物理分、渲染图与模型不符、严重偏离语义 → 直接取消成绩 |
| 提交格式 | submission.zip：manifest.json + 每题目录{model.glb 主模型, metadata.json, process.md, renders/front\|side\|top\|perspective.png}；UTF-8；单位 mm |
| 决赛复现要求 | 断网环境 `bash run_infer.sh /input/prompts.jsonl /output`；权重预打包；单题限时 |
| 题库 | 开发集 40（dev_*）/ 测试集 40（hid_*，**提交按此评分**）/ 校准集 20~30（不公开） |
| 京东卡活动 | 前 10 队提交送 200 元卡——已无名额（页示 12 队提交） |

**测试集题目画像**（40 题，四类别各 10）：小型器物/功能结构/装饰造型/约束生成。全部要求 no_floating_parts；34 题 single_component；32 题 stable_base；14 题 watertight_required；尺寸语义：16 题硬性上限、18 题建议尺寸、6 题无。

---

## 2. 技术方案（已定，勿轻易推翻）

**一句话**：4090 机器（租极智算）跑腾讯开源 Hunyuan3D-2 批量生成 → 拿回笔记本做 FDM 后处理 + 四视图渲染 + 官方格式打包。

- **生成端** `cloud/generate_hy3d.py`：hy3dgen 管线 = 中文文生图(HunyuanDiT) → 形状(默认 tencent/Hunyuan3D-2mini，4090 可换完整版 2) → 纹理(可选，失败不阻塞几何落盘)。参数默认 steps=30/octree=320/guidance=5.5，seed=42+i。输出 `models_raw/<pid>/raw.glb + gen_meta.json`。**未实测，上机先 `--limit 2` 校准接口**
- **后处理** `pipeline/postprocess.py`：去浮渣(体积<1%主体) → merge_vertices/去退化面 → fill_holes×3 → 仍不水密则 pymeshfix 重建 → fix_normals → 底面贴地 z=0 → 尺寸策略 → 减面≤12万 → GLB
- **尺寸解析** `parse_size_constraints`（build_submission.py 内）：子句级解析中文 prompt；高度/限高→z 轴、厚度→min(x,y,z)、宽度/直径→max(x,y)、整体/长度→最长边；"建议约X毫米"→目标尺寸(取多条建议最大值)；上限词表=不超过/不得超过/不大于/不高于/以内/≤/限高/限宽/限长；毫米/mm/厘米/cm 换算；有效域 5~500mm
- **渲染** `pipeline/render_views.py`：matplotlib 纯 CPU，front(azim -90)/side(0)/top(elev 90)/perspective(-55,25)，1024px，纹理 UV 质心采样着色，无 OpenGL 依赖
- **打包/校验** `pipeline/build_submission.py`：build（逐题后处理+渲染+metadata+process.md+manifest+zip）/ validate（≥90% 门槛、必需文件、模型可加载）
- 尺寸兜底：默认目标 80mm，全局上限 --max-mm 150（prompt 解析出的更严限制优先生效）

## 3. 本机资源状态

- 笔记本：AMD R5 4600U + AMD 核显，**无 N 卡** → 只做后处理/打包，生成必须外接 GPU
- Python 3.14.3；已装：trimesh 5.1 / pymeshfix / manifold3d / fast-simplification / numpy / pillow / matplotlib
- SSH 工具：OpenSSH 10.2 可用；专用密钥对 `~/.ssh/autodl_hy3d(.pub)`；plink/pscp 0.85 在 `%LOCALAPPDATA%\Temp\putty-tools`（密码方式首次连接用，任务结束应清理）

## 4. 工作区文件地图（D:\文本生成3D方案赛\）

| 路径 | 说明 |
| --- | --- |
| `README.md` | 项目总览+三条 GPU 路线+命令速查 |
| `pipeline/postprocess.py` `render_views.py` `build_submission.py` | 本地 CPU 管线（已端到端验证） |
| `cloud/generate_hy3d.py` | 4090 机器批量生成脚本（待上机实测） |
| `cloud/setup_autodl.sh` `setup_linux_4090.sh` | Linux 环境安装脚本 |
| `cloud/README_cloud.md` `README_local4090.md` | AutoDL 手册 / 本地4090手册 |
| `prompts/dev/` | 开发集 40 题（已校验） |
| `prompts/test/` | 测试集 40 题 hid_*（已校验） |
| `models_raw/sample_001/` | 官网示例题的构造测试模型 |
| `output/` | sample 验证产物（submission.zip + build_report.json） |
| `带到4090电脑/` | U盘携带包：脚本+prompts.zip+《操作步骤-零基础版.txt》 |
| `租极智算GPU-你只需做这3步.txt` | 用户租机指南（当前主线） |

## 5. GPU 路线与当前状态

- **主线（用户已选）**：租极智算 jygpu.com —— 4090 约 1.6~2 元/时；实例列表复制"SSH登录命令"+密码发 ZCode → **ZCode 远程全程接管**（装环境→试跑→全量→下载→打包→提醒关机）。镜像选 `PyTorch 2.3.0 + CUDA 12.4.1 + py3.10`。文档 doc.jygpu.com
- 备选：4090 电脑 U 盘带走（`带到4090电脑/` 零基础指南）
- 已弃用：AutoDL（用户明确不用）
- 用户状态：0 基础——给用户的一切指引必须逐步骤+成功判据+失败"拍照发我"；用户已有向公司运维要 GPU 机器的动议（已代拟说明话术），未落地

## 6. 验证记录

- sample_001 构造脏模型端到端：碎块 2→1、水密修复成功（轻量路径）、底部贴地、80mm、四视图渲染正常、validate 100% 通过
- 尺寸解析器对 40 题全量核对通过（曾漏"限高"句式，已修）；apply_size_policy 三组单测通过（厚度帽/高度帽/组合帽）
- 云端脚本未跑过：hy3dgen 接口按官方 README 编写，存在版本漂移风险 → 上机第一件事试跑校准

## 7. 下一步（按优先级）

1. **等用户发极智算 SSH 登录命令 + 密码** → 立即接管：nvidia-smi 验卡 → 装依赖(torch cu124 + requirements) → 上传脚本题目 → `--limit 2` 试跑 → 全量测试集 → zip 拉回本机 `models_raw_test/`
2. 向用户要：平台上的**队伍ID/队名**（build_submission 的 manifest 用）
3. 9/6 首次提交（每天 1 次机会）；9/7 根据结果迭代补跑
4. 可选增强：纹理（云端装 requirements_texture + 自定义光栅化）、完整版 shape 模型、octree 380
5. 若入围 top10：封装 run_infer.sh 断网复现 + FDM 实物打印

## 8. 风险与提醒

- 剩余提交机会极少（每天 1 次，至 9/8）：先 validate 全绿再上传
- 云端 hy3dgen 接口漂移是当前最大技术风险，试跑时逐个 import 核对
- 平台要求组队/提交均在 PC 端操作
- 比赛数据仅限比赛用途，禁止商用
