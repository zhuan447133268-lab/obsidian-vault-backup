---
title: 09-22 交接（官方要求独立复核 + 阶段1实测方案已定，等用户租4090）
---

# 2026-09-22 会话交接（Claude 会话 6a173b2f）

> 前置阅读：[[文本生成3D方案赛-主索引]]、[[session-handoff-3d-2026-09-21]]（该文件已由 Zcode 更新至第二十一节，含 09-22 收口总览 §19~21）。
> 本文件记录 **09-22 中午用户与 Claude 的对话结论**：官方 v1.0 独立复核结果、阶段 1 残差风险的最终方案、纹理问题定论、今晚租机执行方案、D 盘空间账。
> **当前状态一句话：交付物已齐备（Zcode 第七次重打 = 当前有效版：源码包 8,959,510 B / 86 条目，答辩版 PPT），死线 09-23 24:00；下一步 = 用户租 4090（选可装 Docker 的 VM 型）→ 贴 SSH+密码 → 阶段 1 全量实测 + 默认值杠杆测量 → 可能合并第八次重打 → 上传百度云盘 + 回填问卷。**

---

## 一、官方 v1.0 要求独立复核（本 session 逐条实测，非转述 Zcode 结论）

对 `D:\wsl\v2repro40\output\`（交付镜像内 `--network none` 真实输出）逐项程序化验真：

| 官方条目 | 结果 |
| --- | --- |
| §1 提交物 5 项 | ✅ 全齐（源码 zip 85 条目/无权重；镜像 tar + Dockerfile 双路；23 文件双源地址+逐文件 SHA256；复现说明文档模板 3.1~3.5 无删节；方案文档与 PPT 同口径） |
| §2.1 入口/断网/失败日志 | ✅ `run_infer.sh` 接口逐字一致，离线默认开，失败写 `_logs/` 非 0 退出 |
| §2.2 输入 | ✅ 逐行 json.loads 按 key 取值，字段顺序无关、多余字段容忍（40 题含嵌套 constraints 实证） |
| §2.3 输出结构 | ✅ 40 目录 + manifest + `_logs/`；metadata **8 必填字段 40/40 全齐**（逐题程序化验的） |
| §2.4 几何硬性 | ✅ trimesh 全量实测 40 glb：最短边 [3.01, 94.49]、最长边 [45, 180] mm 全在 [2,300]；z_min 40/40=0；水密 40/40；无单位立方体归一化 |
| §3.3/3.4 申报 | ✅ 无 LLM 不适用已说明；`commercial_api_used=false` 抽验一致 |
| §4 自查清单 | ✅ 附 A 逐项对照带证据 |
| 密钥扫描 | ✅ 85 条目全扫，唯一命中是 PPT HTML 内嵌 base64 字体/图片数据的随机串（`AKiAD6d…`，非 AWS 格式），零风险 |

**三处"解读性"事项（不违规，被从严问时怎么答）**：
1. process.md"10 小节"= 10 个条目式小节（编号/模型/改进/参数/种子/耗时/后处理/失败修正/商业API/数据来源），非 10 个带标题章节
2. 输出多一个 `logs/` 目录（与 `_logs/` 内容一致的兼容副本，脚本头部与 04 文档均如实写明）
3. 阶段 1（GPU 生成）未在交付镜像内重跑（本机无 N 卡），已四处主动披露——见下节

## 二、阶段 1 残差风险——用户已拍板租 4090 实测（⚠️ 与 Zcode 并行结论有冲突，新 session 先对表）

**用户在本 session 亲口确认的两个关键事实**：① 初赛租的极智算 4090 **能装 Docker**；② 同意按"租机实测"闭合阶段 1 残差风险。

**但与 Zcode 上午的 [[zcode-gpu前提复核与交接-20260922]] 存在两处冲突，新 session 第一步必须先对表**：
1. **实例能否装 Docker**：Zcode 实测"便宜档 4090（¥1.57/h）是容器实例、装不了 Docker" ↔ 用户说初赛那台能装 → ** likely 初赛租的是 VM 型/不同档** → 租机时**必须选可装 Docker 的机型**（租前问客服确认，或选 KVM/VM 型）
2. **容器路线的镜像本体问题**：Zcode 指出交付镜像是**两段式**（`base_prep.sh` + `docker commit`）造的，**按 Dockerfile 重建 ≠ 交付镜像本体** → 若走 Docker 路线，理想做法是**把交付 tar 传上实例 load 后跑**（受 22.2G/上行 0.22MB/s≈29h 制约）→ 若实例网络允许，更实际的是**实例上直接按两段式重现镜像**（base_prep.sh + commit，脚本都在）或确认 Dockerfile 尾段与 commit 差异点
3. **Zcode 的备选方案已就绪**：若装不了 Docker → "锁版本等价环境（同轮子+同 requirements-lock.txt）+ `unshare -n` 真断网 + 官方入口全量 40 题"，工具在 **工作区外 `D:\wsl\tmp\gpu_verify\`**（一键脚本+40 题输入+双基线+操作手册 `GPU实测-交接与决策.md`），成本 ¥3~5、30 分钟开跑。注意 Zcode 强调这**不是**第 20 节否掉的"走源码"路径（源码对照是可选步骤，默认关）

**本 session 定下的实测目标（与 Zcode 方案兼容，孰先孰后由新 session 按机型定）**：
- P0：阶段 1 全量 40 题在**真 GPU + 真断网**下跑通（Docker 容器内最佳，锁环境+unshare 次之）—— tonight's core evidence
- P1：产物 vs 初赛提交版对拍
- P2：纹理链 1~2 题可行性 + 渲染一致性（决赛素材；纹理权重 15G 后台并行下载）
- 租 6h 窗口 ~10-15 元；红线：门禁不过就停/单进程/密码只在对话/先关机再释放

**重要变更（Zcode 并行产出，本 session 复核时确认）**：
- **第七次重打已发生**：用户指令"9/23 交付包要与答辩 PPT 一致，直接出答辩版"→ 13 页 deck 就地重做（11 主讲+2 附录）→ 源码包现为 **8,959,510 B / `58e1d2d1…` / 86 条目**（旧值 `10290691…`/85 条目已作废）；PPT 两版 784,451/784,435 B、`05_方案文档.md` 16,236 B——**本 session 第一节复核时引用的旧哈希以此为准作废**，但复核结论（结构/字段/几何）不受影响
- **"默认值杠杆"待决策**（[[决赛高分抓手-默认值杠杆-20260922]]）：官方跑的是我们的默认值，`FDM_SETTLE_LAST=1`（必做）/`MAX_FACES→20万`/`RENDER_SIZE→1536`（先测）等改动 = 改官方跑分；**GPU 实测若 9/23 02:00 前全绿 → 与默认值改动合并为第八次重打**
- 交付物**不是"冻结零改动"状态**（本 session 早前的表述已过时）：当前 = 第七次重打后 stable，默认值杠杆 + GPU 证据 pending 第八次

## 三、纹理问题定论（用户问"初赛没做纹理能否弥补"）

- **复现窗口（09-23 24:00 前）：不补、不动**。物理上来不及（改管线=重传 22.2G≈29h）；纹理链从未端到端验证，塞进官方断网镜像=放大风险；且初赛弱项是语义不是纹理
- **决赛窗口（10-08~31）：可补**。今晚租机顺带验证（下载 15G 纹理权重在 40 题实测期间后台并行，~20 min 无增量成本）；通过则作为决赛答辩"持续改进"叙事
- **红线提醒**：四视图渲染器是 matplotlib UV 质心采样（按无纹理设计），纹理进 GLB 后必须先验**渲染一致性**——否则撞"渲染图与模型不符"反作弊红线。纹理验证第一优先级是渲染一致性，不是生成

## 四、D 盘空间账（昨天 185G → 今 8G 飘红）

**元凶 = 昨天 Zcode 的 WSL2 docker build**：`D:\wsl\ubuntu\ext4.vhdx` = **117 GB**（基础镜像 15G + 交付镜像 60.5G + 构建中间层）。

| 项目 | 体积 | 处置 |
| --- | --- | --- |
| ext4.vhdx | 117G | **现在不动**（万一今晚实测翻车，本机镜像/build 缓存可 25 min 重导出）；**09-23 上传完成后** docker rmi + 压缩 vhdx → 回收 ~95G |
| 交付 tar.gz | 23G | 保留（上传用） |
| hy3d_weights | 18G | 权重权威副本，等镜像删除后一并压缩 |
| pagefile.sys | 41G | 系统页面文件，非项目产生，另行决策 |
| **99-归档待删** | **9.9G** | 09-16 标了"待删"，**现在就能删，等用户点头** |
| **WPS清理大师文件迁移** | **18G** | WPS 迁移残留（含全人星球课程文件，用户定过"只归档不删"）→ 可挪移动硬盘，**现在就能挪** |

- 与 3D 项目无关即可动：99 归档删除 + WPS 迁移挪走 ≈ **28 GB**
- **对今晚租机计划无影响**（本机只收几十 MB 小文件；分片输出默认在 C 盘 `C:\百度网盘上传`，C 盘余 46G 够）
- 后台全盘 du 扫描（brcsm356l / dusage2.txt）会话结束时仍在跑，已知部分：01-项目 3.9G；新 session 如需完整数据重跑 `du -sh /d/*/`

## 五、新 session 接手动作（按序）

0. **先对表**（最高优先）：读 [[zcode-gpu前提复核与交接-20260922]] 与 [[决赛高分抓手-默认值杠杆-20260922]]，与本文件第二节的冲突表对齐——用户说"初赛那台 4090 能装 Docker"，Zcode 说"便宜档是容器实例装不了"→ 租机前先确认机型
1. **等用户贴极智算 SSH + 密码**（租 4090、数据盘 ≥80G、**选可装 Docker 的 VM 型机型**，租前问客服）→ 按本文件第二节目标执行；若装不了 Docker → 直接用 Zcode 备好的 `D:\wsl\tmp\gpu_verify\` 锁环境方案（¥3~5）
2. 执行时先读 `cloud/上机避坑清单.md` + `cloud/租机执行清单.md` + `D:\wsl\tmp\gpu_verify\GPU实测-交接与决策.md`
3. 实测完：证据落 `实测记录/`；**是否重打（第八次）按 Zcode 判定线**：9/23 02:00 前 GPU 证据全绿 + 默认值杠杆测量达标 → 合并重打；否则维持第七次重打版
4. **09-23 24:00 死线**：上传 `复现提交包/上传百度云盘/`（**以第七次重打后的最新版为准**）到百度云盘（分享选**永久**）→ 回填问卷② → 队长填问卷①；上传完成后做 D 盘回收（第四节表）
5. 顺手问组委会 7+2 问（见 09-21 交接 §21.5 + "评测机驱动版本""材料可否更新"）
6. 答辩准备：09-30 入围后按 09-21 交接 §23 的新 deck 页序 + `决赛答辩准备\答辩讲者手册.md` 准备；素材：FDM 实物、Demo 录屏、今晚纹理验证结果
