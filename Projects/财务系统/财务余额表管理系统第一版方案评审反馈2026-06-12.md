# 财务余额表管理系统 — 三维度评审报告

## 一、项目概述

**系统定位**：集团银行账户余额核对管理系统，用于替代线下Excel采集流程，实现园所银行余额数据的在线填报、区域审核与集团汇总。

**技术栈**：Go 1.26 + Gin + GORM | Vue 3 + TypeScript + Element Plus | MySQL 8.0 | Oracle BI（只读同步）

**核心流程**：
```
Oracle BI → 同步账面余额 → 出纳填报实际余额 → 区域审核确认 → 集团汇总关账
```

---

## 二、架构师评审（评分：6/10）

### 优点

| 维度 | 评价 |
|------|------|
| 技术选型 | Go+Vue+MySQL 主流组合，Oracle仅做只读数据源，职责清晰 |
| 分层设计 | Handler→Service→Repository 三层架构，结构清晰 |
| 状态机 | `pending→draft→submitted→region_confirmed→region_submitted` 设计合理 |
| 部署 | Docker Compose多阶段构建，镜像体积小 |

### 严重问题

| 严重度 | 问题 | 位置 |
|--------|------|------|
| **致命** | `config.yaml` 明文存储Oracle密码、MySQL密码、企微CorpSecret，且提交到Git | config.yaml |
| **致命** | JWT Secret为硬编码占位符`change-this-secret-key-in-production` | config.yaml:16 |
| **高** | `/auth/dev-login` 生产环境可用，任何人可通过工号直接获取管理员JWT | main.go:74 |
| **高** | CORS `Allow-Origin: *` + `Allow-Credentials: true`，违反规范 | cors.go:7 |
| **高** | Oracle SQL表名通过`fmt.Sprintf`拼接，存在注入风险 | sync_service.go:297 |
| **高** | 全局变量`database.DB`被所有层直接引用，无法单元测试 | database.go:17 |
| 中 | Go版本号`1.26.4`不存在（最新稳定版1.22.x） | go.mod:3 |
| 中 | `initDefaults()`每次启动执行，包含数据库写入操作 | main.go:195 |

---

## 三、财务总监评审（业务逻辑合规性）

### 正面评价

| 维度 | 评价 |
|------|------|
| 职责分离 | 出纳填报→区域审核→集团关账，三级权限清晰 |
| 状态机设计 | 余额记录状态流转合理，有明确校验 |
| 关账控制 | 关账后禁止修改，集团管理员可反关账 |
| 操作日志 | 记录操作人、时间、IP，审计链路完整 |
| 差异分析 | 支持填写`diffReason`，便于追溯原因 |

### 业务风险

| 严重度 | 问题 | 业务影响 |
|--------|------|----------|
| **高** | 区域管理员权限校验TODO未完成，可越权操作其他区域数据 | 水平越权，数据泄露 |
| **高** | 同步操作会覆盖已填报的`book_amount`，导致出纳基于旧数据填写的差异分析失效 | 财务数据准确性受损 |
| **高** | 区域提交无事务保护，中途失败导致部分记录状态不一致 | 账实不符 |
| **高** | `balance_record_history`表从未写入，修改历史审计功能未实现 | 无法追溯修改记录 |
| 中 | 同步操作竞态条件，可能创建多个"running"批次 | 数据重复 |
| 中 | 区域提交硬编码10000条上限，超出部分不会被更新 | 数据遗漏 |
| 中 | 无金额校验（如负数、超大数值），可能导致异常数据 | 报表失真 |

### 财务合规性评价

| 合规项 | 状态 | 说明 |
|--------|------|------|
| 职责分离 | ✅ | 出纳、区域管理员、集团管理员权限分离 |
| 操作留痕 | ⚠️ | 有操作日志，但修改历史表未实现 |
| 关账控制 | ✅ | 关账后禁止修改，但反关账无额外审批 |
| 数据准确性 | ⚠️ | Oracle同步可能覆盖已填报数据 |
| 审批流程 | ⚠️ | 区域提交无事务，可能部分成功 |
| 对账功能 | ✅ | 支持账面余额vs实际余额对比 |

---

## 四、测试工程师评审（评级：D+）

### 测试覆盖率：0%（严重）

**整个项目零测试文件**，搜索`*_test.go`、`*.test.ts`、`*.spec.ts`均为空。

对于财务系统，零测试意味着：
- 无法验证业务逻辑正确性
- 修改代码无法确认是否引入回归缺陷
- 无法验证权限控制是否按预期工作

### 安全漏洞（8项高危）

| # | 漏洞 | 位置 |
|---|------|------|
| 1 | 配置文件硬编码敏感凭证 | config.yaml |
| 2 | CORS全开+凭证 | cors.go:7 |
| 3 | DevLogin生产环境暴露 | main.go:74 |
| 4 | JWT Token可通过URL传递 | jwt.go:24 |
| 5 | 默认管理员密码admin123 | main.go:239 |
| 6 | JWT Secret强度不足 | config.yaml:16 |
| 7 | 无登录频率限制 | 全局 |
| 8 | Oracle SQL表名拼接 | sync_service.go:297 |

### 并发与数据一致性问题

| 问题 | 影响 |
|------|------|
| 同步操作无并发锁 | 可能创建多个running批次 |
| 区域提交无事务保护 | 部分记录状态不一致 |
| 同步覆盖已填报数据 | 财务数据准确性受损 |

---

## 五、综合评分

| 维度 | 评分 | 说明 |
|------|:----:|------|
| 技术架构 | 7/10 | 选型合理，但安全问题严重 |
| 分层设计 | 6/10 | 结构清晰但全局DB变量名存实亡 |
| 业务逻辑 | 6/10 | 流程合理，但有TODO未完成 |
| 安全性 | 3/10 | 多个致命级漏洞 |
| 测试覆盖 | 0/10 | 零测试 |
| **综合** | **5/10** | **不建议当前状态用于生产** |

---

## 六、改进建议（按优先级）

### P0 — 上线前必须修复

1. **敏感信息外移**：密码/密钥迁移到环境变量，config.yaml加入.gitignore，轮换已泄露凭证
2. **禁用dev-login**：仅在debug模式启用，生产环境返回404
3. **修复CORS**：限定实际前端域名，去掉`Allow-Credentials: *`
4. **完成区域管理员权限校验**：实现TODO中的区域归属校验
5. **修复同步覆盖问题**：同步时检查记录状态，已提交的不应覆盖book_amount

### P1 — 短期改进

6. **添加事务管理**：区域提交、批量操作包裹在事务中
7. **引入依赖注入**：移除全局database.DB，支持单元测试
8. **Oracle连接池化**：启动时创建连接池，同步过程复用
9. **实现修改历史**：向`balance_record_history`表写入数据
10. **添加并发锁**：同步操作使用数据库锁或唯一索引

### P2 — 中期改进

11. **补充单元测试**：核心业务逻辑测试覆盖率>80%
12. **添加金额校验**：负数、超大数值、精度校验
13. **优化性能**：CountByStatus改为GROUP BY，CSV导出流式处理
14. **添加速率限制**：认证端点防暴力破解
15. **健康检查增强**：检测MySQL连通性

---

**结论**：该项目架构设计合理，但存在多个安全致命漏洞和业务逻辑缺陷。在完成P0修复前，不建议部署到生产环境。
