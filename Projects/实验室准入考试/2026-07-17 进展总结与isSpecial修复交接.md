---
date: '2026-07-17'
tags:
  - session-handoff
  - lab-exam
title: 2026-07-17 实验室准入考试进展与 isSpecial 修复交接
---
# 2026-07-17 实验室准入考试进展与 isSpecial 修复交接

## 今日完成（commit 均在 feat/art-design-pro-migration 分支）

| 事项 | Commit | 验证 |
|---|---|---|
| 首次登录强制改密（本地不强制，接CAS后恢复） | `c11394a` | E2E 12/12 |
| README 同步方案B说明 | `a7f92fd` | - |
| 学校管理员设置（3接口+页面升级） | `a2b69bb` | E2E 13/13 |
| 学院管理员候选人截断修复 | `a8a1c21` | - |
| 防切屏强制交卷（3次LEAVE） | `948c855` | E2E 14/14 |
| 本地验证环境+报告+人工走查清单 | `b7c0524` | 产物 E2E 14/14 |
| admin 页面隐身+改强密码（用户封存） | `5b42729` | 后端 10/10 |
| 学校管理员最少保留 2 名真人 | `8cd761a` | 后端 11/11 |
| 搜索表单 el-select 宽度塌缩修复 | `66f0325` | 截图+E2E |
| 人员管理增强（单个新增+启禁用+findAll隐身） | `f8dc6ab` | 后端16/16，E2E 15/15 |

## 其他事实

- 沈春洋（BN100005453）、陈冰冰（IME000290）已建号并分配 SCHOOL_ADMIN，初始密码 123456
- admin 新密码由用户封存（16位强密码）；E2E 需 `E2E_ADMIN_PASSWORD` 环境变量
- 全量用户 `need_change_password=false`（接 CAS 后执行 `UPDATE users SET need_change_password = true` 恢复）
- 服务：后端 :3000（dist/main）/ 前端 dev :5173 / preview :4173

## 🔴 进行中：isSpecial 假开关修复（下次接续）

**背景**：学院管理页"特殊学院"开关是假开关——抽题逻辑硬编码学院编码 `['QC','DQ','ZN']`，不看 isSpecial 字段；且三学院 isSpecial 数据全为 false 从未初始化。特殊学院业务含义：汽车/电气/智科组卷时除通用安全题+专业题外，还要加通识教育题（三部分）。用户拍板只修假开关（编辑改弹窗、TS/TS1 重复学院两项不做）。

**已完成**：数据已修（QC/DQ/ZN → true）；代码已改两处（exam.service.ts / student-exam.service.ts 抽题判断改用 `config.isSpecialCollege`，常量已删）；后端已编译重启。

**未完成（下次从这里接）**：
1. 验证脚本 `server/verify_is_special.js` 卡在组卷查询为空：batchGeneratePapers 已确认是同步（exam.service.ts:267-321）。下一步：打印 batch-generate 响应的 `generatedCount/skippedCount`；排查学生班级与考试班级是否匹配（脚本取的是 collegeId=DQ 第一个学生，班级可能不一致导致 1200 校验失败或跳过）
2. 代码改动**未提交、全量 E2E 未跑**（当前应为 15 例）
3. 通过后：提交 + 更新 memory

详细排查指南见 memory：`session-handoff-2026-07-17-ispecial-fix-in-progress`


---

# 2026-07-20 收尾：isSpecial 修复完成 ✅

- **卡点根因**：验证脚本跨班取学生（classId ≠ 考试班级），batchGeneratePapers 强校验班级（exam.service.ts:296）→ 1200 整场 400；脚本未打印响应导致表现为"组卷为空"
- **修复**：脚本改 `pickClassWithStudent`（班级+该班学生组合）+ 打印 batch-generate 响应
- **验证**：verify_is_special.js 8/8；全量 E2E 15/15
- **已提交** `ec9ef7e`（exam.service / student-exam.service / verify_is_special.js）
- 工作区剩余未跟踪文件（import 脚本、备份 sql、题库模板 xlsx）为 7-17 数据层遗留，与本次修复无关，未提交


- **同日追加**：学院管理编辑态交互修复，commit `72a88db`（E2E 16/16）——编辑中按钮置灰显示「编辑中」+行高亮，保存/取消后恢复；切换编辑目标/新增时表单 dirty 弹确认，不再静默丢弃未保存修改；AppTable 透传 row-class-name

- **同日追加2**：操作列排版修复，commit `9e73d34`——列宽 140→170 + nowrap，「编辑中」置灰按钮与删除按钮保持并排（截图实证+E2E 2/2）

- **同日追加3**：导入模板下载为空文件修复，commit `fa3de8e`——根因：`get()` 封装二次解包 `res.data` 得 undefined，下载文件为 9 字节字符串 "undefined"；基础数据模板+题库模板两处同 bug 同修（浏览器实证 PK 头有效 xlsx）；E2E 16/16
- **事实确认**：基础数据导入 = 单 Excel 三表（组织架构/教师/学生），仅 SCHOOL_ADMIN 可操作（路由 meta + 后端 @Roles 双重限定）；模板表头为英文字段名（org_code/staff_no 等）+示例行，⚠️ 是否改中文表头待用户决策（改表头需同步改导入解析）

- **同日追加4**：基础数据导入中文化+查库兜底，commit `615ff6f`——中文表头+中文枚举（学院/专业/班级、启用/禁用、在读），英文旧模板双兼容；预载 DB 组织架构，只填人员表也能导入；verify_import.js 8/8（含下游：人员管理可搜、教师候选人可筛）；E2E 16/16。用户确认的终态：学校管理员自助上传Excel→人员管理可搜→学院管理员/组卷老师可筛

- **同日追加5**：导入模板下拉化，commit `4bb652a`——用户拍板：编码列「编码（名称）」Excel下拉（动态取DB）、组织架构sheet从模板去掉；模板=填写说明+教师+学生+隐藏选项表；示例行移入说明页（防误导入）；导入自动截取括号前编码；verify 14/14，E2E 16/16。⚠️ 遗留缺口：新增专业/班级无页面入口（学院管理页只管学院），待用户决策是否补专业/班级管理页

- **同日追加6**：「专业/班级管理缺口」乌龙澄清+修复，commit `39ea381`——页面（majors.vue/classes.vue）与后端 CRUD **早已存在且完整**（编码自动生成：学院码+2位序号/专业码+2位序号，与用户拍板的默认规则一致；删除有班级/用户约束），仅菜单漏挂；补「组织管理」分组菜单（专业管理/班级管理）+E2E 17/17。🔴 坑复用：E2E 下拉必须 :visible 限定（又踩一次）

- **同日追加7**：专业/班级页编辑体验修复，commit `553ed25`——① 表单卡片在表格底部（点编辑像没反应）→ 挪顶部+滚动定位（三页统一，E2E 又揭一层：长表格滚下去点编辑表单在视口外）② 编辑提交漏字段（专业漏 collegeId、班级漏 grade/majorId，改了不生效）③ 两页套用编辑中/dirty确认模式；E2E 17/17。新坑沉淀：E2E 用例名加随机后缀防失败残留撞名

- **同日追加8**：班级表单必填显性化+spec去脆弱化，commit `6c571a4`——年级等三项加必填红星（用户拍板：必填合理，防静默脏数据连锁污染统计/导入兜底）；排查插曲：4个考试流E2E突挂1101，根因是用户在页面试编辑把23电气一班年级填了2023，年级排序把它从list[0]挤掉（84/85班级grade为空），spec「取学院班级列表第一个」模式脆弱 → 4个spec统一改按 T001 带课关系（teacher-classes接口）取班级。教训：E2E 取数不能依赖列表排序

- **同日追加9**：班级年级批量回填完成——84/85 个班级 grade 为空（7-15 原始导入未带年级），按班级名前两位解析年份回填（23xxx→2023），干跑 84/84 可解析 0 异常、编码/名称年份一致后落库，剩余空年级 0；回填前已备份 classes 表（lab_exam_backup_classes_before_grade_backfill_20260720_135238.sql）；E2E 17/17。学生侧 120 人年级本就齐全无需处理

- **同日追加10**：学院管理员分配班级报1101根因查明+UX修复，commit `a8fc1a3`——根因：唐丽丽管辖 TS1(空壳0班0人)+自身 TS(4人0班)，两学院都无班级，选任何班都被正确拦截；下拉无范围过滤显示全校85班是体验缺陷。修复：listClasses 按角色加数据范围（admin全量/学院管理员限管辖/教师限带课班），弹窗空列表改明确提示。实证 admin=85/唐丽丽=0/T001=1；E2E 17/17。⚠️ 遗留业务问题：①通识学院教师无班级可分，组卷/监考业务路径待与沈老师确认 ②TS/TS1 重复学院合并待确认（已咬人一次）

- **同日追加11**：学校管理员「教师分班管理」上线，commit `c1ce4d1`——复用本院教师页（菜单/路由+标题随meta），解决通识学院教师跨班授课设置问题：由学校管理员挂任意学院班级（现有TeacherClass多对多模型天然支持，零业务规则变更）；顺带修教师候选人截断 pageSize 100→500；E2E 18/18。TS/TS1 重复学院合并仍待沈老师确认

- **同日追加12**：TS/TS1 重复学院合并完成（用户确认为同一学院）——保留 TS（通识教育学院），唐丽丽 CollegeAdmin 关系 TS1→TS，删除空壳 TS1，学院总数 9→8；备份 `lab_exam_backup_before_ts_merge_20260720.sql`；E2E 18/18。至此组织数据层面的遗留问题清零
