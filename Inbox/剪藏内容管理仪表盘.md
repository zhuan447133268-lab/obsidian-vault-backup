---
# Dataview 剪藏管理仪表盘

description: 汇总从小红书/抖音/B站/公众号等渠道剪藏到 Obsidian 的内容
tags:
  - dashboard
  - dataview
  - clipping
---

# 📥 剪藏内容管理仪表盘

> 这个仪表盘依赖 **Dataview** 插件。如果还没安装，去社区插件市场搜索 `Dataview` 安装启用。

---

## 🔔 待整理（inbox 状态）

```dataview
TABLE WITHOUT ID
  file.link AS "笔记",
  source AS "来源",
  topic AS "主题",
  author AS "作者",
  date AS "日期",
  rating AS "评分"
FROM "Inbox" AND #clipping
WHERE status = "inbox"
SORT date DESC
```

---

## 📊 按来源平台汇总

```dataview
TABLE WITHOUT ID
  source AS "来源",
  rows.file.link AS "笔记",
  length(rows) AS "数量"
FROM #clipping
WHERE source != ""
GROUP BY source
SORT length(rows) DESC
```

---

## 📅 最近 7 天新增

```dataview
TABLE WITHOUT ID
  file.link AS "笔记",
  source AS "来源",
  topic AS "主题",
  summary AS "一句话总结"
FROM #clipping
WHERE date >= date(today) - dur(7 days)
SORT date DESC
```

---

## ⭐ 高价值内容（rating >= 4）

```dataview
TABLE WITHOUT ID
  file.link AS "笔记",
  source AS "来源",
  topic AS "主题",
  rating AS "评分",
  summary AS "一句话总结"
FROM #clipping
WHERE rating >= 4
SORT rating DESC, date DESC
```

---

## 🏷️ 按主题分布

```dataview
TABLE WITHOUT ID
  topic AS "主题",
  rows.file.link AS "笔记",
  length(rows) AS "数量"
FROM #clipping
WHERE topic != ""
GROUP BY topic
SORT length(rows) DESC
```

---

## 📁 全部剪藏内容

```dataview
TABLE WITHOUT ID
  file.link AS "笔记",
  source AS "来源",
  type AS "类型",
  status AS "状态",
  topic AS "主题",
  date AS "日期",
  rating AS "评分"
FROM #clipping
SORT date DESC
```

---

# 🛠️ 使用说明

## 1. 新建剪藏笔记时

复制 `模板-剪藏笔记.md` 的内容，或者配合 **Templater** 插件自动套用模板。

必填 frontmatter 字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| `source` | 来源平台 | `wechat`, `xiaohongshu`, `douyin`, `bilibili`, `zhihu` |
| `type` | 内容类型 | `article`, `video`, `post`, `chat` |
| `status` | 处理状态 | `inbox`（待整理）, `processing`（处理中）, `archived`（已归档） |
| `topic` | 主题分类 | `AI教育`, `财务系统`, `教学方法` |
| `author` | 原作者 | `维客笔记` |
| `url` | 原文链接 | `https://mp.weixin.qq.com/s/...` |
| `date` | 剪藏日期 | `2026-07-14` |
| `rating` | 价值评分 1-5 | `4` |
| `summary` | 一句话总结 | 用一句话概括这篇文章对你的价值 |

## 2. 存放路径建议

```
你的OB库/
├── Inbox/
│   └── 笔记同步助手/        ← 笔记同步助手/Cubox 自动落这里
│       ├── 2026-07-14-文章标题.md
│       └── ...
├── Clippings/               ← 手动整理后的归档位置（可选）
│   ├── AI教育/
│   ├── 财务系统/
│   └── 教学方法/
├── Excalidraw/              ← 你画的图
├── Dashboards/              ← 这个仪表盘文件放这里
│   └── 剪藏内容管理仪表盘.md
└── Templates/               ← 模板放这里
    └── 剪藏笔记模板.md
```

## 3. 整理工作流

```
手机剪藏 → 自动落入 Inbox/笔记同步助手/
    ↓
每天/每周打开「剪藏内容管理仪表盘」
    ↓
把 status=inbox 的内容逐一处理：
    - 补充 summary
    - 打 topic 标签
    - 评分 rating
    - 提取行动项
    - 把 status 改为 archived
    - 必要时移动到 Clippings/主题/ 下
```

## 4. 和 Claude Code 配合

你可以直接对 Claude Code 说：

> 「帮我整理 Inbox/笔记同步助手/ 下所有 status=inbox 的剪藏笔记，给每篇补充 summary、topic 和 rating，并把 status 改成 processing。」

Claude Code 可以直接读取你的 Obsidian 库文件并批量修改 frontmatter。

---

# 📝 配套 Templater 模板（可选）

如果你装了 **Templater** 插件，可以把下面这段存为模板文件，新建剪藏时自动填充日期：

```markdown
---
source: 
type: article
status: inbox
topic: ""
author: ""
url: ""
date: <% tp.date.now("YYYY-MM-DD") %>
rating: 
summary: ""
tags:
  - clipping
---

# <% tp.file.title %>

> 来源： | 作者： | 日期：<% tp.date.now("YYYY-MM-DD") %>
> 原文链接：

## 一句话总结


## 核心内容


## 我的思考/行动项

- [ ] 

## 相关笔记

- [[ ]]
```
