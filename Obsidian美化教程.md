# Obsidian 美化与功能搭建教程

> 本文档记录了将 Obsidian 打造成「Crisp Pulse」风格知识库的完整操作步骤，包括外观美化、Canvas 架构图、思维导图、数据看板四个模块。

---

## 模块一：外观美化（渐变背景 + 卡片排版）

### 效果
柔粉蓝渐变背景 + 半透明白色卡片式正文区域。

### 操作步骤
1. 将 CSS 文件放到仓库的 `.obsidian/snippets/` 目录下
2. 文件名：`crisp-gradient.css`
3. 打开 Obsidian → `设置 → 外观 → CSS 代码片段`
4. 点刷新按钮，打开 `crisp-gradient` 开关

### 关键设置
- `设置 → 编辑器 → 可读行宽`：**关闭**（否则内容被锁在 700px）
- `设置 → 外观 → 主题`：使用**浅色主题**

### 可调参数
- 卡片宽度：CSS 中 `max-width: 1400px`
- 卡片透明度：`rgba(255, 255, 255, 0.62)` 最后一个数字（越大越白）
- 渐变颜色：CSS 开头 `linear-gradient(...)` 中的色值

### 深色紫色主题（备选）
- 文件：`.obsidian/snippets/purple-night.css`
- 效果：深紫渐变背景 + 深紫半透明卡片 + 紫色标题
- 用法：切深色主题后，启用 `purple-night`，关闭 `crisp-gradient`
- 文字颜色已全套覆盖（表格、侧边栏、正文、代码块均为浅紫白色）

---

## 模块二：Canvas 知识架构图

### 效果
中心节点 + 多分支彩色卡片 + 连线标签的系统架构图。

### 文件位置
- `知识架构.canvas`：纯文本卡片版架构图
- `系统架构.canvas`：文件节点版架构图（引用真实笔记）
- `架构模块/` 文件夹：各模块的详细笔记

### 操作方法
1. 在左侧文件列表双击打开 `.canvas` 文件
2. **双击卡片文字**编辑内容（纯文本卡片）
3. **拖动卡片**移动位置
4. 从卡片**右下角圆点拖到另一卡片**加连线
5. **框选多卡片 → 右键 → Group** 分组上色
6. **右键空白处 → 插入卡片**加新节点

### 文件节点版（高级用法）
- 把笔记文件从左侧列表**直接拖进 Canvas**，方框里会显示笔记的实际内容
- 笔记改了，Canvas 自动更新
- 这就是"系统架构图"的做法：每个方框是一篇真实笔记
- 示例：`系统架构.canvas` 引用了 `架构模块/` 下的 4 篇笔记

---

## 模块三：思维导图

### 效果
把笔记标题大纲自动渲染成左右展开的思维导图。

### 安装插件
1. `设置 → 第三方插件 → 浏览`
2. 搜索 **Enhancing Mindmap**，安装并启用

### 使用方法
1. 用 `#` `##` `###` 标题组织笔记层级
2. 按 `Ctrl + P` 打开命令面板
3. 选 **`Enhancing Mindmap: 创建新思维导图`**（或在编辑模式下选"切换为 markdown 或 mindmap 模式"）
4. 思维导图中：
   - `Tab`：加子节点
   - `Enter`：加同级节点
   - 直接打字编辑节点内容

---

## 模块四：数据看板

### 效果
统计表格 + GitHub 风格热力图 + 最近笔记 + 文件夹分布。

### 前置插件
1. **Dataview**：安装启用
2. **Heatmap Calendar**：安装启用

### 关键设置
1. `设置 → 第三方插件 → Dataview → 选项`
2. 打开以下开关：
   - **Enable JavaScript queries**
   - **Enable inline JavaScript queries**
3. 完全重启 Obsidian（退出再打开）

### 文件位置
- `知识看板.md`（仓库根目录）

### 看板内容
- **核心指标卡片**：顶部一排大数字卡片（总笔记数、本周新建、本周编辑、总字数、标签数）
- **活跃热力图**：GitHub 风格年度色块图（卡片容器包裹）
- **最近笔记**：最近 10 天编辑的笔记列表（卡片容器包裹）

### 卡片样式说明
- 卡片样式定义在 `crisp-gradient.css` 第 11 节（`.crisp-*` 类名）
- 统计卡片用 DataviewJS 生成 HTML，通过 `dv.container.innerHTML` 渲染
- 热力图和表格用 callout 语法 `> [!note]` 包裹成卡片外观
- 不要在笔记里写 `<style>` 标签（Obsidian 不会解析，会显示成代码）

---

## 模块五：深色指挥中心主页

### 效果
深色主题下的启动页：统计小卡片 + 快捷按钮组 + 最近笔记，像应用首页。

### 前置设置
1. **切深色主题**：`设置 → 外观 → 主题`，选深色主题
2. **启用 CSS**：`设置 → 外观 → CSS 代码片段`，打开 `command-center`

### 文件位置
- `指挥中心.md`（仓库根目录）
- `.obsidian/snippets/command-center.css`（深色卡片样式）

### 页面内容
- **统计小卡片**：本周新建、本周编辑、待处理、总笔记（DataviewJS 自动统计）
- **快捷按钮组**：新建笔记、搜索、知识图谱、看板、架构图、思维导图
- **最近笔记列表**：自动列出最近编辑的 8 篇笔记

### 注意
- 这个模块和浅色渐变主题互斥：用指挥中心时切深色，用日常笔记时切浅色
- 两套 CSS snippet 可以同时开着，不冲突

---

## 文件清单

| 文件 | 说明 |
|---|---|
| `.obsidian/snippets/crisp-gradient.css` | 浅色渐变 + 看板卡片 CSS |
| `.obsidian/snippets/purple-night.css` | 深色紫色渐变主题 CSS |
| `.obsidian/snippets/command-center.css` | 深色指挥中心卡片 CSS |
| `知识架构.canvas` | Canvas 纯文本架构图 |
| `系统架构.canvas` | Canvas 文件节点版架构图 |
| `架构模块/` | 各模块详细笔记 |
| `思维导图示例.md` | 思维导图示例笔记 |
| `知识看板.md` | 卡片式数据看板（浅色） |
| `指挥中心.md` | 深色指挥中心主页 |

---

## 常用快捷键

- `Ctrl + P`：打开命令面板
- `Ctrl + E`：切换编辑/阅读模式
- `Ctrl + R`：重载 Obsidian
